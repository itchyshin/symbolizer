% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warnings.R
\name{warning_table}
\alias{warning_table}
\title{Per-fit prose warnings for a symbolized model}
\usage{
warning_table(x, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_warning_table}) with columns
\code{code}, \code{severity}, \code{message}, \code{context}.
}
\description{
Returns the tibble of conditions \code{symbolize()} flagged when it built
the model object. Each row is one warning with a \code{code} (machine
identifier), a \code{severity} (\code{info} / \code{warn} / \code{error}), a \code{message} (the
prose templated from \code{inst/extdata/warning-templates.csv}), and a
\code{context} string naming which slot values triggered the rule.

Today the system ships two checks:
\itemize{
\item \code{few_groups_wald} \emph{(warn)} — Wald 95\% CI used with a random-effect
group of fewer than ~10 levels.
\item \code{small_n_wald} \emph{(info)} — Wald CI on a small dataset. Currently
defined in the template CSV but reserved; no extractor fires it yet
(the threshold is fit-dependent and not always informative).
}

New conditions are added by appending a row to
\code{inst/extdata/warning-templates.csv} and wiring the trigger in the
relevant \code{drm_build_warnings()} (or sibling for other classes).
}
