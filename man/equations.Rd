% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-latex.R
\name{equations}
\alias{equations}
\title{Equation rows from a symbolized_model}
\usage{
equations(x, notation = c("index", "matrix", "both"), ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{notation}{One of \code{"index"}, \code{"matrix"}, or \code{"both"}. Sets the display
preference recorded as the \code{"notation"} attribute and used by the
\code{symbolizer_equations} \code{print()} method.}

\item{...}{Reserved for future use.}
}
\value{
A tibble with columns \code{name}, \code{kind}, \code{submodel}, \code{index}, \code{matrix}
and the additional class \code{"symbolizer_equations"}.
}
\description{
Returns the equation lines carried by a \code{\link[=new_symbolized_model]{symbolized_model}}
as a tibble. One row per renderable block in \code{x$components}. Both notations
(index and matrix) are always returned in their own columns; the \code{notation}
argument only controls how the result is \emph{displayed} by \code{\link[=print]{print()}}.

For the LaTeX-string form ready to splice into a document, see
\code{\link[=as_latex]{as_latex()}}.
}
\examples{
# equations(symbolize(fit))
}
