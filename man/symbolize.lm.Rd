% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-base.R
\name{symbolize.lm}
\alias{symbolize.lm}
\title{Symbolize a base R lm() fit (Gaussian, v0.10 first slice)}
\usage{
\method{symbolize}{lm}(fit, symbols = NULL, units = NULL, context = NULL, ...)
}
\arguments{
\item{fit}{A fitted statistical model object.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_model} object.
}
\description{
Builds a \code{\link[=new_symbolized_model]{symbolized_model}} from a base R
\code{lm} object. v0.10 covers the Gaussian conditional submodel only
(the only family \code{lm()} fits). The residual standard deviation
(\code{summary(fit)$sigma}) is exposed as the \code{sigma} variance component
but is not modelled -- \code{lm()} doesn't support distributional
regression on sigma.
}
\section{Confidence intervals}{

CIs come from \code{stats::confint(fit)}. The method is "Wald-t" -- the
usual frequentist t-based CI you'd see in \code{summary(fit)}.
\code{ci_method} is set to \code{"wald"} for consistency with the other
frequentist extractors.
}

