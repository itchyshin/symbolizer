% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-three-views.R
\name{as_html_three_views}
\alias{as_html_three_views}
\alias{as_html_three_views.symbolized_model_set}
\title{Three-views HTML rendering of a symbolized_model}
\usage{
as_html_three_views(
  x,
  head = 5L,
  tail = 2L,
  head_cols = 5L,
  tail_cols = 2L,
  id = "sym",
  standalone = FALSE,
  file = NULL,
  ...
)

\method{as_html_three_views}{symbolized_model_set}(
  x,
  head = 5L,
  tail = 2L,
  head_cols = 5L,
  tail_cols = 2L,
  id = "sym",
  standalone = FALSE,
  file = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{symbolized_model} with \verb{$expanded} populated.}

\item{head}{Number of leading \strong{rows} to show in the matrix view
(Tab 3 "Equations with data") before the \verb{\\\\vdots} ellipsis.
Default \code{5}. Reasonable range 2--10; larger values produce taller
widgets.}

\item{tail}{Number of trailing \strong{rows} to show in the matrix view
after the \verb{\\\\vdots}. Default \code{2}. Together with \code{head}, controls
how much per-observation data is visible: total rows shown =
\code{head + tail + 1} ellipsis row (or fewer if \code{n_obs} is small
enough to show all rows).}

\item{head_cols}{Number of leading \strong{columns} to show in any matrix
that has more than \code{head_cols + tail_cols} columns (e.g. the
\code{Z}-matrix for a 60-species fit). Default \code{5}. Smart-truncation
prefers non-zero columns within the visible row band.}

\item{tail_cols}{Number of trailing \strong{columns} to show after the
\verb{\\\\cdots} ellipsis. Default \code{2}.}

\item{id}{A short identifier so multiple panels can co-exist on one page.}

\item{standalone}{If \code{TRUE}, wrap the fragment in a full HTML document
with a MathJax CDN bootstrap so the file renders math when opened
directly in a browser (\verb{file://...}). Default \code{FALSE} returns the
bare fragment for embedding in an Rmd / pkgdown / Quarto page where
the host document already loads MathJax.}

\item{file}{If non-\code{NULL}, also write the HTML to this path. Returned
value is unchanged.}

\item{...}{Reserved for future use.}
}
\value{
A character vector (HTML), invisible.
}
\description{
Returns a single self-contained HTML string with three tabs over the same
fit:
\enumerate{
\item \strong{Equation} -- matrix-form structural equations.
\item \strong{Index} -- per-observation equations.
\item \strong{Matrix (with data)} -- the actual numeric arrays from the fit, with
head + tail rows visible and \code{...} in the middle.
}

Designed to be \code{cat()}-ed inside an Rmd / Quarto chunk with
\code{results = 'asis'}. The host document supplies math rendering (MathJax /
KaTeX via pandoc); this function emits semantic HTML, inline CSS, and a
small tab-switching script.
}
\details{
For a multi-node structural equation model (a \code{symbolized_model_set},
the shared parent of \code{piecewiseSEM}'s \code{symbolized_psem} and drmSEM's
\code{symbolized_drm_sem}), the per-node three-views widget is rendered for
each node and stacked under a \verb{<h2>Node: <name></h2>} header. Nodes are
read from \code{names(x$parts)}, so the same method serves either collator.
}
