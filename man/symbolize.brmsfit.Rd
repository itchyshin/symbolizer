% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-brms.R
\name{symbolize.brmsfit}
\alias{symbolize.brmsfit}
\title{Symbolize a brms fit (Gaussian, v0.8 first slice)}
\usage{
\method{symbolize}{brmsfit}(fit, symbols = NULL, units = NULL, context = NULL, ...)
}
\arguments{
\item{fit}{A fitted statistical model object.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_model} object.
}
\description{
Builds a \code{\link[=new_symbolized_model]{symbolized_model}} from a \code{brmsfit}
object. v0.8 covers the Gaussian conditional submodel (identity link)
with optional \code{(1 | g)} random intercepts. Other families and brms's
distributional-parameter formulas (\code{sigma ~ z}, etc.) return capability
errors via \code{\link[=capability_check]{capability_check()}}.
}
\section{Credible intervals}{

brms is Bayesian, so the "confidence band" carried in
\code{fixed_effects} and \code{interpretation} is a 95\% \strong{credible interval} --
the 2.5\% and 97.5\% posterior quantiles. \code{excludes_zero} is the
indicator for whether the credible interval excludes zero (i.e. the
posterior puts negligible mass on either side of zero). \code{ci_method}
is set to \code{"credible"} so renderers can label the band correctly.
}

\references{
Bürkner, P.-C. (2017). brms: An R Package for Bayesian Multilevel
Models Using Stan. \emph{Journal of Statistical Software}, 80(1), 1-28.
}
