% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-three-views.R
\name{as_pdf_three_views}
\alias{as_pdf_three_views}
\title{PDF rendering of a symbolized_model in three stacked sections}
\usage{
as_pdf_three_views(
  x,
  file,
  title = NULL,
  head = 5L,
  tail = 2L,
  head_cols = 5L,
  tail_cols = 2L,
  keep_tex = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{symbolized_model}.}

\item{file}{Output path (\code{.pdf}). Required.}

\item{title}{Optional document title. Defaults to a short auto-title
built from the fit's class and response.}

\item{head}{Number of leading rows shown in any matrix block before
the \verb{\\\\vdots} ellipsis (default \code{5}).}

\item{tail}{Number of trailing rows shown after the \verb{\\\\vdots} (default \code{2}).}

\item{head_cols}{Number of leading columns shown in any matrix block
with more than \code{head_cols + tail_cols} columns (default \code{5}).
Smart-truncation prefers non-zero columns within the visible row band.}

\item{tail_cols}{Number of trailing columns shown after the \verb{\\\\cdots}
ellipsis (default \code{2}).}

\item{keep_tex}{If \code{TRUE}, keep the intermediate \code{.tex} next to the PDF
for inspection. Default \code{FALSE}.}

\item{...}{Passed to \code{rmarkdown::render()}.}
}
\value{
The path to the rendered PDF (invisibly).
}
\description{
Paper-ready PDF counterpart to \code{\link[=as_html_three_views]{as_html_three_views()}}. The same three
"views" of one fitted model, laid out vertically as three sections on
one PDF page (no tabs):
\enumerate{
\item \strong{Index form} -- the per-observation equations.
\item \strong{Matrix form} -- the same equations in matrix notation.
\item \strong{Worked observation} -- the index-form equations evaluated at
observation i = 1 of the data, showing the coefficient estimates
plugged in and the predicted / residual decomposition.
}

Rendered via \code{rmarkdown::render()} with \code{output_format = "pdf_document"};
a working LaTeX install (TinyTeX or system TeX) is required.

The wide matrix-of-numbers view from the HTML widget's third tab is
deliberately not included -- on PDF the bmatrix of n x p numbers
overflows a portrait A4 page. The worked-row at i = 1 carries the
same teaching content in a fits-on-one-page form.
}
