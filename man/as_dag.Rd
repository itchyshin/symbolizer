% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as-dag.R
\name{as_dag}
\alias{as_dag}
\title{Model diagram for a symbolized model}
\usage{
as_dag(x, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{...}{Reserved for future use.}
}
\value{
A list classed \code{c("symbolic_dag", "list")} with three
slots: \code{nodes} (tibble), \code{edges} (tibble), \code{dot} (single
character string with the GraphViz / DOT representation).
}
\description{
Returns a structural DAG of the fitted model: nodes for the response,
each predictor, each parameter (mu, sigma, ...), and any random-effect
grouping variables; edges connecting predictors to the parameters they
enter, parameters to the response, and groups to their random-effect
contribution. The returned object is an S3 list with \code{nodes} and
\code{edges} tibbles plus a generated DOT-language string.

Rendering live needs a downstream tool that consumes DOT - the
\code{DiagrammeR} package (which symbolizer does NOT require) is one
option; pasting \code{as_dag(sym)$dot} into \url{https://dreampuf.github.io/GraphvizOnline/}
is another. The S3 print method shows the DOT string and a brief
summary; \code{as_dag(sym)$nodes} and \verb{$edges} are the raw tables.

This is a structural surface - it visualises \emph{how} the symbolic
object connects predictors, parameters, and the response. It does
not show coefficient values; for those see
\code{\link[=parameter_interpretation]{parameter_interpretation()}}.
}
