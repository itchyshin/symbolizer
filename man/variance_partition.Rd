% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-partition.R
\name{variance_partition}
\alias{variance_partition}
\title{Where the variation lives: a variance partition for a mixed model}
\usage{
variance_partition(x, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}} whose underlying fit
is retained on \code{x$metadata$fit}.}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_variance_partition}) with one row per
variance source. Columns: \code{component}, \code{variance}, \code{sd}, \code{pct}. Carries a
\code{reason} attribute (character or \code{NA}) explaining an all-\code{NA} \code{pct}.
}
\description{
\code{variance_partition()} turns a fitted mixed model's variance components into
a one-row-per-source table: each random-effect variance plus, when it is
defined, the residual (within-group) variance. The \code{pct} column is each
source's share of the total, answering a biologist's first question of a
mixed model -- \emph{where does the variation live?}

The residual row (and therefore \code{pct}) appears only when the residual
variance is defined on a single scale: a homoscedastic Gaussian fit (one
residual SD) or a binomial fit (the known latent-scale residual,
\eqn{\pi^2/3} for logit, 1 for probit). For other families, or a
location-scale Gaussian whose residual SD varies across observations, the
random-effect variances are still shown but \code{pct} is \code{NA} with a reason --
shares of a total are not meaningful without a residual.

Point estimates only; no confidence intervals (the package's uncertainty
contract).
}
\seealso{
\code{\link[=icc]{icc()}} for the single-number intraclass correlation.
}
