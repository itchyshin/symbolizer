% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-piecewisesem.R
\name{symbolize.psem}
\alias{symbolize.psem}
\title{Symbolize a piecewiseSEM \code{psem} fit}
\usage{
\method{symbolize}{psem}(fit, symbols = NULL, units = NULL, context = NULL, ...)
}
\arguments{
\item{fit}{A \code{piecewiseSEM::psem()} fit.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_psem} object with elements \code{parts} (list of
per-node \code{symbolized_model}s, named by response variable),
\code{node_names} (character), \code{cov_arcs} (character, declared \verb{\%~~\%}
arcs as \code{"a ~~ b"} strings), and \code{metadata}.
}
\description{
Walks the per-node fits inside a \code{piecewiseSEM::psem()} object and
delegates each to \verb{symbolize.<class>()}, returning a \code{symbolized_psem}
collator that bundles the per-node \code{symbolized_model} objects, the
node names (response variables, in declared order), and any declared
\verb{\%~~\%} residual covariance arcs.
}
\details{
Each node is mean-structure only by construction. For distributional
(location-scale) or multivariate structural equations, use the
\code{drm_sem} path in the drmSEM package.
}
