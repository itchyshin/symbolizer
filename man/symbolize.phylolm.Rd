% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-phylolm.R
\name{symbolize.phylolm}
\alias{symbolize.phylolm}
\title{Symbolize a phylolm fit (Gaussian PGLS, v0.21.4 first slice)}
\usage{
\method{symbolize}{phylolm}(fit, symbols = NULL, units = NULL, context = NULL, data = NULL, ...)
}
\arguments{
\item{fit}{A fitted statistical model object.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{data}{Optional data frame. If \code{NULL}, falls back to
\code{fit$model.frame} (phylolm stores it there).}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_model} object.
}
\description{
Builds a \code{\link[=new_symbolized_model]{symbolized_model}} from a \code{phylolm}
object. v0.21.4 covers BM and Pagel's lambda evolutionary models;
OU / EB / kappa / delta / trend produce a \code{symbolized_model} but
carry the model name in \code{metadata$phylo_model} for downstream
diagnostics rather than full template support.
}
\details{
phylolm fits the PGLS marginal form \verb{y = X*beta + e, e ~ N(0, sigma^2 * C(alpha))} where \code{C(alpha)} is a tree-derived correlation matrix. There
is no explicit \code{u_p} random-effect — the phylogenetic signal lives
entirely in the residual covariance. \code{metadata$phylo_representation}
is set to \code{"pgls_marginal"} so downstream renderers can phrase this
correctly.
}
\section{Confidence intervals}{

phylolm reports Wald-type standard errors via \code{vcov(fit)}. The
\code{confint_low} / \code{confint_high} columns of \code{fixed_effects} are
computed as \verb{estimate +/- 1.96 * std_error}, and \code{ci_method} is set
to \code{"wald"}.
}

\references{
Ho, L.S.T. & Ané, C. (2014). A linear-time algorithm for Gaussian
and non-Gaussian trait evolution models. \emph{Systematic Biology},
63(3), 397-408.

Tung Ho, L.S. & Ané, C. (2014). Intrinsic inference difficulties for
trait evolution with Ornstein-Uhlenbeck models. \emph{Methods in Ecology
and Evolution}, 5(11), 1133-1146.
}
