% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand.R
\name{expand}
\alias{expand}
\title{Expand a symbolized_model to its underlying numeric arrays}
\usage{
expand(x, ...)
}
\arguments{
\item{x}{A \code{symbolized_model}.}

\item{...}{Reserved for future use.}
}
\value{
A list with elements (NULL when not applicable to this fit):
\itemize{
\item \code{y}         response vector of length n
\item \code{X}         mu-submodel design matrix (n x p_mu)
\item \code{beta}      mu coefficient vector (p_mu)
\item \code{X_sigma}   sigma-submodel design matrix (n x p_sigma)
\item \code{gamma}     sigma coefficient vector (p_sigma)
\item \code{Z_g}       random-effect indicator matrix (n x G), if RE present
\item \code{u}         random-effect BLUPs (G), if RE present
\item \code{e}         residuals (n)
\item \code{mu_hat}    fitted conditional mean (n)
\item \code{sigma_hat} fitted per-observation SD (n)
}
}
\description{
\code{expand()} returns the actual numeric pieces that make up the fitted model:
the response vector, design matrix per submodel, coefficient vectors,
random-effect indicator matrix and BLUPs (when present), residuals, and the
fitted conditional mean and SD vectors. This is the data side of the
\code{\link[=new_symbolized_model]{symbolized_model}} surface — the symbolic and matrix
views show \emph{what shape} the model has; \code{expand()} shows \emph{what numbers}
actually flow through it.

Returned object is classed \code{c("symbolized_expanded", "list")} so it has a
compact \code{print()} method.
}
