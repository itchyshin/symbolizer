% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal-estimates.R
\name{group_contrasts}
\alias{group_contrasts}
\title{Pairwise and reference contrasts between factor levels}
\usage{
group_contrasts(
  x,
  by = NULL,
  method = c("pairwise", "trt.vs.ctrl", "revpairwise", "consec", "poly"),
  within = NULL,
  scale = c("response", "link"),
  ci_method = NULL,
  adjust = c("none", "tukey", "mvt"),
  ...
)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}} whose underlying fit is
retained on \code{x$metadata$fit}.}

\item{by}{Character vector of factor name(s) whose levels are contrasted.
Defaults to all factors in the model.}

\item{method}{Contrast family, passed to \code{\link[emmeans:contrast]{emmeans::contrast()}}:
\code{"pairwise"} (all pairs, default), \code{"trt.vs.ctrl"} (each level versus the
reference), \code{"revpairwise"}, \code{"consec"} (consecutive levels), or \code{"poly"}.}

\item{within}{Optional factor name(s) to compute the contrasts \emph{within each
level of} (emmeans \code{by}). Use this for the multi-factor case: pairwise
contrasts of one factor separately at each level of another.}

\item{scale}{One of \code{"response"} (default) or \code{"link"}. On \code{"link"} the
contrast is an additive difference on the linear-predictor scale; on
\code{"response"} it is back-transformed (a ratio for log links, an odds ratio
for logit links).}

\item{ci_method}{Confidence-interval method label; defaults to
\code{x$metadata$ci_method}. See \code{\link[=group_means]{group_means()}}.}

\item{adjust}{Multiplicity adjustment for the confidence bands: \code{"none"}
(default; one per-contrast interval), \code{"tukey"} (family-wise), or \code{"mvt"}.
This only widens the interval -- it never produces a p-value.}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_group_contrasts}) with one row per
contrast. Columns: \code{contrast}, one column per \code{within} factor, \code{level_combo},
\code{estimate}, \code{std_error}, \code{confint_low}, \code{confint_high}, \code{excludes_zero},
\code{ci_method}, \code{scale}, \code{method}, \code{adjust}, \code{effect_type}.
}
\description{
\code{group_contrasts()} compares the levels of a factor against each other
(or against a reference), delegating to \code{\link[emmeans:contrast]{emmeans::contrast()}}. Use it for
any multi-level factor where the coefficient table -- which only reports each
level versus the reference -- leaves "which levels differ from each other?"
unanswered.

Each row is one contrast with a point estimate, standard error, 95\%
confidence band, and an \code{excludes_zero} flag. \strong{No p-values are reported}:
symbolizer shows compatibility bands, not tests. For families on a log or
logit link the response-scale contrast is a \emph{ratio} (or odds ratio), whose
null value is 1, not 0 -- the \code{effect_type} column records which, and the
\code{excludes_zero} flag is computed against the correct null.

Not supported on bivariate Gaussian (\code{biv_gaussian}) fits -- see
\code{\link[=group_means]{group_means()}} for the limitation and alternatives.
}
\examples{
d <- transform(mtcars, gear = factor(gear))
sym <- symbolize(lm(mpg ~ gear, data = d))
if (requireNamespace("emmeans", quietly = TRUE)) {
  # which gears differ from each other (not just from the reference)?
  group_contrasts(sym, by = "gear")
}
}
\seealso{
\code{\link[=group_means]{group_means()}}, \code{\link[=group_slopes]{group_slopes()}}
}
