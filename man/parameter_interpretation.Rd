% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-interpretation.R
\name{parameter_interpretation}
\alias{parameter_interpretation}
\title{Per-parameter interpretations for a symbolized model}
\usage{
parameter_interpretation(
  x,
  scale = c("all", "link", "natural", "variance", "biological"),
  ...
)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{scale}{One of \code{"all"} (default), \code{"link"}, \code{"natural"}, \code{"variance"},
or \code{"biological"}. \code{"all"} returns every reading column; the other values
return just the identifier columns (\code{submodel}, \code{term_label},
\code{coefficient_role}, \code{estimate}) plus the matching single reading column.}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_interpretation}) with \code{submodel},
\code{term_label}, \code{coefficient_role}, \code{estimate}, and one or more reading
columns. The \code{"scale"} attribute records which scale was requested.
}
\description{
Returns the per-coefficient reading tibble carried by a
\code{\link[=new_symbolized_model]{symbolized_model}} as a reader-friendly table. Each
row corresponds to one fixed-effect coefficient and pairs it with the prose
readings already templated by \code{symbolize()}: a link-scale reading, a
natural-scale reading, an optional variance-scale reading (for scale
submodels), and a biological reading.

The renderer is a pure consumer of \code{x$interpretation}. All prose was
substituted from \code{inst/extdata/interpretation-templates.csv} at extract
time; this surface only displays it.
}
\examples{
# parameter_interpretation(symbolize(glm(am ~ wt, binomial(), data = mtcars)))
}
