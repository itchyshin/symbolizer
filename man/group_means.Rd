% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal-estimates.R
\name{group_means}
\alias{group_means}
\title{Per-group marginal means for a symbolized model}
\usage{
group_means(x, by = NULL, ci_method = NULL, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}} whose underlying fit
is retained on \code{x$metadata$fit}.}

\item{by}{Optional character vector of factor names to marginalize over.
Defaults to all factors in the model.}

\item{ci_method}{Confidence-interval method. Defaults to the \code{ci_method}
stored on \code{x$metadata$ci_method} so the band matches the symbolize()
call. emmeans currently produces asymptotic Wald-style intervals
regardless of this argument, but the column is propagated for
consistency with \code{\link[=parameter_interpretation]{parameter_interpretation()}}.}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_group_means}) with one row per
level combination. Columns: \code{level_combo}, one column per factor in
\code{by}, \code{estimate}, \code{std_error}, \code{confint_low}, \code{confint_high},
\code{excludes_zero}, \code{ci_method}.
}
\description{
\code{group_means()} returns categorical marginal means for the factors in a
\code{\link[=new_symbolized_model]{symbolized_model}}, delegating to
\code{\link[emmeans:emmeans]{emmeans::emmeans()}} under the hood. Each row is one combination of the
requested factors' levels with a point estimate, standard error, 95\%
confidence band, and an \code{excludes_zero} indicator.

Use this alongside \code{\link[=parameter_interpretation]{parameter_interpretation()}} for any fit that contains
a factor: the coefficient table shows contrasts (differences from the
reference level); \code{group_means()} shows each group's expected response.
}
