% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal-estimates.R
\name{group_slopes}
\alias{group_slopes}
\title{Per-group slopes for a continuous predictor}
\usage{
group_slopes(
  x,
  continuous,
  at = NULL,
  scale = c("response", "link"),
  ci_method = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}} whose underlying fit
is retained on \code{x$metadata$fit}.}

\item{continuous}{Name of the continuous predictor whose slope is wanted.}

\item{at}{How to stratify. Three accepted shapes:
\itemize{
\item \code{NULL} (default): use every factor in the model that interacts with
\code{continuous} (determined from \code{x$terms}).
\item A character vector of factor names: stratify by those factors' levels.
\item A named list (e.g. \code{list(z = c(-1, 0, 1))}): for continuous-by-
continuous interactions, get the slope at those values of \code{z}.
}}

\item{scale}{One of \code{"response"} (default) or \code{"link"}. For
identity-link families the two are equivalent. For other families
the slope is reported on the requested scale: \code{"link"} gives the
linear-predictor slope (which is what the coefficient table shows),
\code{"response"} gives the slope after back-transformation. Note that
for non-identity links the response-scale slope depends on the
level of the predictor.}

\item{ci_method}{Confidence-interval method. Defaults to
\code{x$metadata$ci_method}. See \code{\link[=group_means]{group_means()}} for details.}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_group_slopes}) with one row per
stratum. Columns: \code{predictor}, \code{level_combo}, one column per stratifying
variable, \code{estimate}, \code{std_error}, \code{confint_low}, \code{confint_high},
\code{excludes_zero}, \code{ci_method}, \code{scale}.
}
\description{
\code{group_slopes()} returns the slope of a continuous predictor stratified by
one or more factors (or by values of another continuous predictor),
delegating to \code{\link[emmeans:emtrends]{emmeans::emtrends()}}. Each row is one stratum with a
point estimate, standard error, 95\% confidence band, and an
\code{excludes_zero} indicator.

Use this alongside \code{\link[=parameter_interpretation]{parameter_interpretation()}} for any fit with a
continuous-by-categorical interaction (or continuous-by-continuous): the
coefficient table reports contrast slopes (differences from the reference
group's slope); \code{group_slopes()} reports each group's slope directly.

Not supported on bivariate Gaussian (\code{biv_gaussian}) fits — see
\code{\link[=group_means]{group_means()}} for the same limitation and the recommended
alternatives.
}
