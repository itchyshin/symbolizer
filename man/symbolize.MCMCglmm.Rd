% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-mcmcglmm.R
\name{symbolize.MCMCglmm}
\alias{symbolize.MCMCglmm}
\title{Symbolize an MCMCglmm fit (Gaussian, v0.9 first slice)}
\usage{
\method{symbolize}{MCMCglmm}(fit, symbols = NULL, units = NULL, context = NULL, data = NULL, ...)
}
\arguments{
\item{fit}{A fitted statistical model object.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{data}{The data frame used to fit. Required: MCMCglmm does not
keep the data on the fitted object.}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_model} object.
}
\description{
Builds a \code{\link[=new_symbolized_model]{symbolized_model}} from an
\code{MCMCglmm} fit. v0.9 covers the Gaussian conditional submodel
(identity link) with optional \code{~ g} random intercepts. Other
families and MCMCglmm's flexible covariance structures return
capability errors via \code{\link[=capability_check]{capability_check()}}.
}
\details{
MCMCglmm does NOT keep the data frame on the fitted object, so
\code{data} is a mandatory argument here.
}
\section{Credible intervals}{

MCMCglmm is Bayesian. The CI band in \code{fixed_effects} and
\code{interpretation} is a 95\% \strong{credible interval} (\verb{l-95\% CI} /
\verb{u-95\% CI} from \code{summary(fit)$solutions}). \code{ci_method} is set to
\code{"credible"}.
}

\references{
Hadfield, J. D. (2010). MCMC Methods for Multi-Response Generalized
Linear Mixed Models: The MCMCglmm R Package. \emph{Journal of
Statistical Software}, 33(2), 1-22.
}
