% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-recipe.R
\name{simulate_recipe}
\alias{simulate_recipe}
\title{Simulation recipe for a symbolized model}
\usage{
simulate_recipe(x, n = NULL, seed = NULL, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{n}{Optional integer; if supplied, the R code uses \code{n}
observations. Defaults to \code{sym$model$n_obs}.}

\item{seed}{Optional integer; if supplied, the R code begins with
\code{set.seed(seed)}.}

\item{...}{Reserved for future use.}
}
\value{
A list of class \code{symbolizer_simulation_recipe} with
\code{pseudocode} and \code{r_code} character slots, plus \code{metadata} for
the family and submodel decomposition.
}
\description{
Returns a structured recipe -- pseudocode + runnable R code --
explaining how to simulate data \emph{from} a fitted model. The recipe
is family-aware: it picks the right RNG (\code{rnorm}, \code{rbinom},
\code{rpois}, \code{rgamma}, \code{rnbinom}, ...) and lists the draws in the
right order (random effects first, then linear predictor, then
response).

The returned object is an S3 list with \code{pseudocode} (character
vector, one element per step) and \code{r_code} (single character
string, ready to \code{eval(parse(text = ...))}). \code{print()} formats it
for the console; \code{knit_print()} formats it as a fenced R code
block plus a numbered pseudocode list.

This is a teaching surface: the recipe shows the \strong{generative
model}, not the inferential machinery. For a real simulation
that round-trips through the fitted-model object's internals
(sampling from posterior, properly propagating uncertainty), the
relevant package's own \code{simulate()} / \code{posterior_predict()} /
\code{predict()} interface is the right tool.
}
