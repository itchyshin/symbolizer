% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-text.R
\name{methods_text}
\alias{methods_text}
\title{Methods-section paragraph from a symbolized model}
\usage{
methods_text(sym, ...)
}
\arguments{
\item{sym}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{...}{Reserved for future use.}
}
\value{
A list classed \code{c("symbolizer_methods_text", "list")} with
slots \code{text} (the assembled paragraph as a single character string),
\code{slots} (the named substitutions used), and \code{reminders} (character
vector of editorial reminders for the author).
}
\description{
Returns a draft Methods-section paragraph for a fitted model — the kind
of paragraph a biologist would otherwise have to write by hand into a
paper. The prose is composed from CSV templates — the paragraph frame from
(\code{inst/extdata/methods-templates.csv}) and the reusable clause phrases from
(\code{inst/extdata/methods-phrases.csv}) — using slots filled from the
\code{symbolized_model}. No prose is generated by a language model at
runtime; every phrase traces to a template row plus the substituted
slots.

The output is always a \strong{draft}. Treat it as starting material to be
edited: confirm the package version, soften or sharpen the sentences,
add the citation, and drop in any study-specific context. The print
method appends an editorial reminder for that reason.
}
\examples{
\dontrun{
fit <- drmTMB::drmTMB(
  drmTMB::drm_formula(body_mass ~ temperature, sigma ~ temperature),
  family = stats::gaussian(), data = body_mass_dat
)
sym <- symbolize(fit)
methods_text(sym)
}

}
