% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain-factors.R
\name{explain_factors}
\alias{explain_factors}
\title{Explain a model's factors and dummy coding in one call}
\usage{
explain_factors(x, ...)
}
\arguments{
\item{x}{A fitted model supported by \code{\link[=symbolize]{symbolize()}}, or a
\code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{...}{Passed to \code{\link[=symbolize]{symbolize()}} when \code{x} is a fit.}
}
\value{
A \code{symbolized_factor_explanation} object.
}
\description{
\code{explain_factors()} is the recommended entry point for understanding the
categorical predictors in a fit. For every factor it states, in plain
language, the coding scheme (its levels, which level is the reference, and
how many indicator columns it contributes), then shows the per-group means
(\code{\link[=group_means]{group_means()}}) and the pairwise comparisons (\code{\link[=group_contrasts]{group_contrasts()}}).
For every interaction it gives the difference-of-differences reading together
with the inline cell means or per-group slopes.

Pass a fitted model (it is run through \code{\link[=symbolize]{symbolize()}} for you) or an existing
\code{\link[=new_symbolized_model]{symbolized_model}}.

Print the result at the console for a walkthrough, or knit it inside a Quarto
/ R Markdown document for a heading-and-table section per factor.
}
\examples{
d <- transform(mtcars, gear = factor(gear))
if (requireNamespace("emmeans", quietly = TRUE)) {
  # one call: coding scheme, group means, and pairwise contrasts per factor
  explain_factors(lm(mpg ~ gear, data = d))
}
}
\seealso{
\code{\link[=group_means]{group_means()}}, \code{\link[=group_contrasts]{group_contrasts()}}, \code{\link[=explain]{explain()}}
}
