% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-latex.R
\name{as_latex}
\alias{as_latex}
\title{Render a symbolized_model as LaTeX}
\usage{
as_latex(x, notation = c("index", "matrix", "both"), env = "aligned", ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}}.}

\item{notation}{One of \code{"index"}, \code{"matrix"}, or \code{"both"}. With \code{"both"},
two \code{env} blocks are stacked, separated by a \verb{\\text\{(matrix notation)\}}
header.}

\item{env}{LaTeX environment to wrap the equations in. Defaults to
\code{"aligned"}. Other useful values: \code{"align*"}, \code{"gather"}.}

\item{...}{Reserved for future use.}
}
\value{
A single character string.
}
\description{
\code{as_latex()} turns a \code{\link[=new_symbolized_model]{symbolized_model}} into a single
LaTeX string ready to splice into a document. By default the equations are
wrapped in an \code{aligned} environment with \code{&} alignment markers inserted
before \code{=} or \verb{\\sim} on each line.
}
\examples{
# cat(as_latex(symbolize(fit)))
}
