% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-factor-views.R
\name{as_html_factor_views}
\alias{as_html_factor_views}
\title{Interactive HTML widget for a model's factors and dummy coding}
\usage{
as_html_factor_views(x, id = "symfv", standalone = FALSE, file = NULL, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}} (output of
\code{\link[=symbolize]{symbolize()}}).}

\item{id}{Character id stem for the widget's element ids. Default \code{"symfv"}.}

\item{standalone}{Logical; if \code{TRUE}, wrap the fragment in a self-contained
HTML page (with a MathJax bootstrap) suitable for opening directly in a
browser. Default \code{FALSE} (an embeddable fragment).}

\item{file}{Optional path; if given, the HTML is written there and returned
invisibly instead of printed.}

\item{...}{Reserved for future use.}
}
\value{
A character vector (HTML), invisibly.
}
\description{
\code{as_html_factor_views()} renders a four-tab interactive widget that walks a
reader through the categorical structure of a fit: the coding scheme (levels,
reference, indicator columns), the per-group means, the pairwise comparisons,
and the interactions. It is the web-facing companion to \code{\link[=explain_factors]{explain_factors()}}.

Estimates are drawn in the Confidence-Eye grammar -- a pale compatibility
region with a darker interval outline and a hollow point estimate; the
language is confidence/compatibility, never posterior. Tabs 2-4 need
\pkg{emmeans}; without it they show a short note.
}
\examples{
d <- transform(mtcars, gear = factor(gear))
sym <- symbolize(lm(mpg ~ gear, data = d))
# write the interactive widget to a self-contained HTML file
html <- as_html_factor_views(sym, standalone = TRUE,
                             file = tempfile(fileext = ".html"))
}
\seealso{
\code{\link[=explain_factors]{explain_factors()}}, \code{\link[=as_html_three_views]{as_html_three_views()}}
}
