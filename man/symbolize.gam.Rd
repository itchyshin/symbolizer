% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-mgcv.R
\name{symbolize.gam}
\alias{symbolize.gam}
\title{Symbolize an mgcv gam / bam fit (additive grammar, v0.14 first slice)}
\usage{
\method{symbolize}{gam}(fit, symbols = NULL, units = NULL, context = NULL, ...)
}
\arguments{
\item{fit}{A fitted statistical model object.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_model} object.
}
\description{
Builds a \code{\link[=new_symbolized_model]{symbolized_model}} from an
\code{mgcv::gam()} or \code{mgcv::bam()} fit. The first slice covers
gaussian / poisson / binomial / Gamma families with smooth
specifications \code{s(x)}, \code{s(x, by = factor)}, and \code{te(x, z)}.
}
\details{
For \code{mgcv::gamm()} or \code{gamm4::gamm4()}, both return a list with a
\verb{$gam} component of class \code{"gam"}. Pass that component to
\code{symbolize()}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{g <- mgcv::gamm(y ~ s(x), random = list(g = ~1), data = d)
sym <- symbolize(g$gam)
}\if{html}{\out{</div>}}

Correlation structures and lme4-style random effects from \code{gamm} /
\code{gamm4} are reflected via the \verb{$gam} object's smooth and
parametric-coefficient blocks; the GAMM-specific covariance
structures are not separately rendered in v0.14.
}
\section{Confidence intervals}{

Parametric (non-smooth) coefficients in \code{fixed_effects} carry
approximate Wald 95\% CIs computed as estimate ± 1.96 * SE from
\code{summary(fit)$p.coeff} / \code{summary(fit)$se}. Smooth-term inference
is summarised separately in \code{sym$metadata$smooths} (basis
dimension, edf, p-value per smooth) rather than a single
coefficient row.
}

\references{
Wood, S. N. (2017). \emph{Generalized Additive Models: An Introduction
with R} (2nd ed.). Chapman and Hall/CRC.

Wood, S. N. (2011). Fast stable restricted maximum likelihood and
marginal likelihood estimation of semiparametric generalized linear
models. \emph{Journal of the Royal Statistical Society (B)}, 73(1),
3-36.
}
