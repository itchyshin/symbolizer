% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-tables.R
\name{symbol_table}
\alias{symbol_table}
\title{Symbol dictionary as a reader-friendly table}
\usage{
symbol_table(x, notation = c("both", "index", "matrix"), ...)
}
\arguments{
\item{x}{A \code{symbolized_model}.}

\item{notation}{One of \code{"both"} (default), \code{"index"}, or \code{"matrix"}.
\code{"index"} keeps only the element-wise (\code{W_i}, \verb{\\mu_i}) form,
\code{"matrix"} keeps only the bold matrix-form (\verb{\\mathbf\{w\}},
\verb{\\boldsymbol\{\\mu\}}) and drops rows that have no matrix form (e.g.,
continuous predictors whose only symbol is an indexed scalar).}

\item{...}{Reserved for future use.}
}
\value{
A tibble (S3 class \code{symbolizer_symbol_table}) with \code{variable},
\code{units}, \code{role}, \code{dimension}, \code{dimension_concrete}, and \code{description}
columns plus one or both of \code{symbol} and \code{symbol_matrix}.
}
\description{
Returns the symbol dictionary of a \code{\link[=new_symbolized_model]{symbolized_model}}
as a tibble suitable for tabular display. The dictionary lists every
mathematical symbol that appears in the rendered equations (response,
predictors, parameters, coefficients, and design matrices), together with
the original R variable, its units, and an abstract / concrete dimension.

The renderer is a pure consumer of \code{x$symbol_dictionary}: it does not parse
formulas or invent prose. Use \code{notation} to control which symbol column(s)
are shown.
}
