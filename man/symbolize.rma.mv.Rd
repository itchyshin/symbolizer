% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize-metafor.R
\name{symbolize.rma.mv}
\alias{symbolize.rma.mv}
\title{Symbolize a metafor rma.mv fit (multilevel / multivariate meta-analysis, v0.13.1)}
\usage{
\method{symbolize}{rma.mv}(fit, symbols = NULL, units = NULL, context = NULL, ...)
}
\arguments{
\item{fit}{A fitted statistical model object.}

\item{symbols}{Optional named character vector mapping variable names
to user-supplied LaTeX symbols, e.g. \code{c(body_mass = "W_i", temperature = "T_i")}.}

\item{units}{Optional named character vector mapping variable names to
units, e.g. \code{c(body_mass = "g", temperature = "C")}.}

\item{context}{Optional short character description of the model, e.g.
\code{"avian body-size location-scale model"}.}

\item{...}{Reserved for method-specific extra arguments.}
}
\value{
A \code{symbolized_model} object.
}
\description{
Builds a \code{\link[=new_symbolized_model]{symbolized_model}} from an
\code{rma.mv} fit. Covers the multi-tier random-effects structure
(\code{random = list(~ 1 | study, ~ 1 | id)} or nested
\code{~ 1 | district / study}), optional R-matrix structured random
effects (e.g., phylogenetic via \code{R = list(phylo = phylo.cor)}),
and meta-regression moderators.
}
\details{
Each random-effect tier gets a row in \code{variance_components} with
\code{kind = "heterogeneity"} (unstructured) or \code{kind = "structured"}
(when an R-matrix is attached). Tiers with an R-matrix render as
\verb{u_r ~ N(0, sigma^2_r * R_r)} in the LaTeX; structured tiers also
appear in \code{sym$metadata$structured_random} with their matrix
dimension.
}
