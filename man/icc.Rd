% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-partition.R
\name{icc}
\alias{icc}
\title{Intraclass correlation (repeatability) for a mixed model}
\usage{
icc(x, ...)
}
\arguments{
\item{x}{A \code{\link[=new_symbolized_model]{symbolized_model}} whose underlying fit
is retained on \code{x$metadata$fit}.}

\item{...}{Reserved for future use.}
}
\value{
A length-1 numeric (S3 class \code{symbolizer_icc}) with attributes
\code{scale} (\code{"data"}, \code{"latent"}, or \code{NA}), \code{reason} (character or \code{NA}), and
\code{caption} (character or \code{NA}). Prints a one-line reading at the console.
}
\description{
\code{icc()} returns the intraclass correlation -- the proportion of variance
that lies between groups -- for a mixed model with a single random-effect
term. For a biologist this is the \emph{repeatability} of the trait.

The returned value carries a \code{scale} attribute that is essential to its
interpretation:
\itemize{
\item \strong{data scale} (\code{scale = "data"}): a Gaussian-identity fit with one
random intercept. \eqn{\sigma^2_g / (\sigma^2_g + \sigma^2_\varepsilon)}
is a genuine proportion of variance in the observed response.
\item \strong{latent scale} (\code{scale = "latent"}): a binomial fit, where the residual
is the known link-scale constant (\eqn{\pi^2/3} for logit, 1 for probit).
This is repeatability on the latent (link) scale and is \strong{not} a
proportion of variance in the observed 0/1 outcome -- the returned object
carries that caption.
}

When the quantity is not defined -- another family, a location-scale
Gaussian whose residual SD varies, or more than one random-effect term --
\code{icc()} returns \code{NA} with a human-readable \code{reason} attribute rather than a
misleading number. Use \code{\link[=variance_partition]{variance_partition()}} to see the full breakdown in
those cases. Point estimate only; no confidence interval.
}
\seealso{
\code{\link[=variance_partition]{variance_partition()}} for the full source-by-source breakdown.
}
