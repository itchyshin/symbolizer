# Wrap a vector of equation lines in `aligned` environment

Wrap a vector of equation lines in `aligned` environment

## Usage

``` r
wrap_aligned(lines)
```

## Arguments

- lines:

  Character vector of LaTeX equation lines.
