# gllvm syndromes + integrated plasticity widget — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite `vignettes/symbolizer-gllvm.Rmd` around the Nakagawa-paper's syndromes → integrated-plasticity build-up; replace the previous "between-only vs with-uniqueness" widget pair with one Widget 1 (Σ_B = Λ_B Λ_B^T + Ψ_B) and one Widget 2 (adds Σ_W = Λ_W Λ_W^T + Ψ_W); extend `symbolize.gllvmTMB()` and `render-three-views.R` so Tab 3 emits a numerical implied-covariance block plus per-trait repeatability.

**Architecture:** The slice is a thin vertical: one extractor extension (six new slots on `sym$expanded`), one renderer extension (one new LaTeX emitter), one article rewrite (gllvm.Rmd). The renderer remains a pure formatter over `$expanded`; no new abstractions, no API additions. The article carries all the new pedagogical content (univariate motivation, parameter-counting argument, two widgets, glmmTMB bridge sidebar).

**Tech Stack:** R, gllvmTMB (lead path), glmmTMB (bridge mention only), testthat 3 with snapshots, pkgdown, MathJax (via pandoc → MathML; pkgdown default), the existing symbolizer renderer pipeline.

**Spec:** `docs/dev-log/designs/2026-05-28-gllvm-syndromes-integrated-plasticity.md`

---

## File structure

| File | Responsibility | New / modified |
|---|---|---|
| `R/symbolize-gllvmtmb.R` | Extractor; `glm_build_expanded()` populates the new two-tier slots; new helpers `glm_has_within_unit()` and `glm_compute_repeatability()` | Modified |
| `R/render-three-views.R` | Renderer; new `latex_implied_cov_block()` emitter; Tab 3 conditionally appends B and W implied-covariance blocks | Modified |
| `tests/testthat/helper-gllvm-fits.R` | Adds `fit_gllvm_two_tier()` fixture | Modified |
| `tests/testthat/test-symbolize-gllvmtmb-two-tier.R` | TDD home for the new contract | **Created** |
| `inst/extdata/capabilities.csv` | Status rows for `gllvmTMB, gaussian, Sigma_W` and `Psi_W` | Modified |
| `inst/extdata/assumption-templates.csv` | New `Sigma_W` decomposition row | Modified |
| `inst/extdata/family-parameterizations.csv` | `gllvm_gaussian` row notes both Σ_B and Σ_W when present | Modified |
| `vignettes/symbolizer-gllvm.Rmd` | Article rewrite | Modified |
| `docs/articles/symbolizer-gllvm.html` | Built artefact (regenerated by pkgdown) | Regenerated |
| `NEWS.md` | v0.21.6-redo entry | Modified |
| `DESCRIPTION` | Version bump | Modified |

**Dependency order:** extractor (Tasks 1–7) → renderer (Tasks 8–11) → CSV (Tasks 12–14) → article rewrite (Tasks 15–24) → build + audit + ship (Tasks 25–29). Each numbered task ends with a green test (or a green build) and a commit.

**No new R files** — everything fits in existing modules to avoid sprawl. Helpers stay in `symbolize-gllvmtmb.R` because they're gllvmTMB-specific. Renderer helper goes in `render-three-views.R` because it's used only by Tab 3.

---

## Task 0: Preflight verification

**Files:**
- Read-only: `docs/dev-log/designs/2026-05-28-gllvm-syndromes-integrated-plasticity.md`
- Test: ad-hoc R script

- [ ] **Step 1: Re-read the spec end-to-end.** Open the design doc and confirm you understand every sub-bullet of §4 (Widget anatomy), §5 (R syntax + auto-suppression), §6 (Implementation breakdown), and §9 (Done = ?). Make notes on anything unclear; if anything is, raise it before starting.

- [ ] **Step 2: Confirm worktree state.**

Run: `cd "/Users/z3437171/Dropbox/Github Local/symbolizer-hotfix" && git status && git rev-parse --abbrev-ref HEAD`
Expected: branch `hotfix-pattern-O-matrix-truncation`, working tree carrying the v0.21.5-redo widget slice plus the new spec file (uncommitted or committed both fine; just be aware).

- [ ] **Step 3: Verify P2 (round-trip simulation).** Confirm gllvmTMB can fit the two-tier model AND that `extract_communality()` round-trips on the simulated dataset.

Run:

```r
Rscript -e '
suppressMessages(library(gllvmTMB))
set.seed(20260523)
n_ind <- 40L; n_sess <- 3L; n_tr <- 5L
Lam_B <- matrix(c(1.0, 0.0, 0.7, 0.0, 0.6, 0.0, 0.0, 1.0, 0.0, 0.7),
                nrow = n_tr, byrow = TRUE)
psi_B <- rep(0.4, n_tr); sig_eps <- sqrt(0.3)
z_score <- matrix(rnorm(n_ind * 2L), nrow = n_ind, ncol = 2L)
psi_B_resid <- matrix(rnorm(n_ind * n_tr, sd = rep(sqrt(psi_B), each = n_ind)),
                      nrow = n_ind, ncol = n_tr)
mu_ind_trait <- z_score %*% t(Lam_B) + psi_B_resid
dat <- expand.grid(
  trait      = factor(paste0("t", seq_len(n_tr)), levels = paste0("t", seq_len(n_tr))),
  session    = factor(seq_len(n_sess)),
  individual = factor(seq_len(n_ind))
)
dat$value <- mu_ind_trait[cbind(as.integer(dat$individual), as.integer(dat$trait))] +
              rnorm(nrow(dat), sd = sig_eps)
dat$obs <- factor(paste(dat$individual, dat$session, sep = "_"))
e <- new.env(); e$latent <- get("latent", envir = asNamespace("gllvmTMB"))
e$`unique` <- get("unique", envir = asNamespace("gllvmTMB"))
fit <- eval(quote(gllvmTMB(value ~ 0 + trait +
        latent(0 + trait | individual, d = 2) + `unique`(0 + trait | individual) +
        latent(0 + trait | obs, d = 1) + `unique`(0 + trait | obs),
        data = dat, family = stats::gaussian(),
        trait = "trait", unit = "individual",
        unit_obs = "obs", cluster = "session", silent = TRUE)), envir = e)
cat("Lambda_B dim:", dim(fit$report$Lambda_B), "\n")
cat("Lambda_W dim:", dim(fit$report$Lambda_W), "\n")
cat("sigma_eps    :", round(fit$report$sigma_eps, 4),
    " (auto-suppressed if << 0.05)\n")
cat("sd_B         :", round(fit$report$sd_B, 4), "\n")
cat("sd_W         :", round(fit$report$sd_W, 4), "\n")
'
```

Expected: `Lambda_B dim: 5 2`, `Lambda_W dim: 5 1`, `sigma_eps` ≈ 0.001 (auto-suppressed), `sd_B` and `sd_W` are length-5 vectors. If the fit errors or `sigma_eps` is not auto-suppressed, **stop and revisit the spec**; the auto-suppression assumption is load-bearing.

- [ ] **Step 4: Snapshot the current article + extractor state.**

Run: `git log --oneline -5 && git diff --stat HEAD~1 HEAD`
Read the most recent commit message and confirm it's the v0.21.5-redo widget slice (extractor + article + tests).

---

## Task 1: Extractor helper — `glm_has_within_unit(fit)`

Identifies whether the fit has a within-unit (obs-level) reduced-rank or diagonal covstruct. The renderer + extractor branch on this.

**Files:**
- Modify: `R/symbolize-gllvmtmb.R` (add helper function near the existing `glm_has_unique_unit()`)
- Test: `tests/testthat/test-symbolize-gllvmtmb-two-tier.R` (create)

- [ ] **Step 1: Create the new test file with the first failing test.**

Create `tests/testthat/test-symbolize-gllvmtmb-two-tier.R`:

```r
# v0.21.6-redo: tests for the two-tier (Σ_B + Σ_W) widget contract on
# `symbolize.gllvmTMB`. The three-views renderer's Tab 3 needs Lambda_W,
# Z_W, Psi_W, Sigma_W, and a per-trait Repeatability vector.

test_that("glm_has_within_unit() detects an obs-level latent() term", {
  fit <- fit_gllvm_two_tier()
  expect_true(symbolizer:::glm_has_within_unit(fit))
})

test_that("glm_has_within_unit() returns FALSE for a between-only fit", {
  fit <- fit_gllvm_with_unique()  # between only; no obs-level term
  expect_false(symbolizer:::glm_has_within_unit(fit))
})
```

- [ ] **Step 2: Run the test to confirm RED.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-symbolize-gllvmtmb-two-tier.R", reporter = "summary")'`
Expected: error — either `fit_gllvm_two_tier` undefined OR `glm_has_within_unit` undefined.

- [ ] **Step 3: Add the helper to `R/symbolize-gllvmtmb.R`.** Locate `glm_has_unique_unit()` (around line 241 in the current file). Insert below it:

```r
glm_has_within_unit <- function(fit) {
  cs <- fit$covstructs %||% list()
  unit_col <- fit$unit_col %||% ""
  any(vapply(cs, function(e) {
    g_txt <- if (is.null(e$group)) "" else paste(deparse(e$group), collapse = " ")
    identical(e$kind, "rr") && !identical(g_txt, unit_col)
  }, logical(1L)))
}
```

(The test for `fit_gllvm_two_tier()` will still fail in Step 4 until Task 2 lands — that's expected; complete this task on the `fit_gllvm_with_unique()` half first.)

- [ ] **Step 4: Confirm half-GREEN.**

Run the same test command from Step 2.
Expected: the "returns FALSE for a between-only fit" test passes; the first test still errors because `fit_gllvm_two_tier` isn't defined yet — that's fine, Task 2 ships it.

- [ ] **Step 5: Commit.**

```bash
cd "/Users/z3437171/Dropbox/Github Local/symbolizer-hotfix"
git add R/symbolize-gllvmtmb.R tests/testthat/test-symbolize-gllvmtmb-two-tier.R
git commit -m "feat(gllvm): add glm_has_within_unit() detector (v0.21.6-redo, refs #9)

First slice of the syndromes + integrated plasticity widget rewrite.
Detects obs-level reduced-rank covstructs so the extractor + renderer
can branch on whether Σ_W is present."
```

---

## Task 2: Test fixture — `fit_gllvm_two_tier()`

The two-tier fit used by every subsequent test. Mirrors the article's data exactly so widget snapshots stay aligned.

**Files:**
- Modify: `tests/testthat/helper-gllvm-fits.R`

- [ ] **Step 1: Add the fixture to the helper file.** Append:

```r
fit_gllvm_two_tier <- function(seed = 20260523L) {
  testthat::skip_if_not_installed("gllvmTMB")
  set.seed(seed)
  n_ind  <- 40L; n_sess <- 3L; n_tr <- 5L
  Lam_B <- matrix(c(1.0, 0.0, 0.7, 0.0, 0.6, 0.0,
                    0.0, 1.0, 0.0, 0.7),
                  nrow = n_tr, byrow = TRUE)
  psi_B   <- rep(0.4, n_tr); sig_eps <- sqrt(0.3)
  z_score <- matrix(rnorm(n_ind * 2L), nrow = n_ind, ncol = 2L)
  psi_B_resid <- matrix(rnorm(n_ind * n_tr,
                              sd = rep(sqrt(psi_B), each = n_ind)),
                        nrow = n_ind, ncol = n_tr)
  mu_ind_trait <- z_score %*% t(Lam_B) + psi_B_resid
  dat <- expand.grid(
    trait      = factor(paste0("t", seq_len(n_tr)),
                        levels = paste0("t", seq_len(n_tr))),
    session    = factor(seq_len(n_sess)),
    individual = factor(seq_len(n_ind))
  )
  dat$value <- mu_ind_trait[cbind(as.integer(dat$individual),
                                   as.integer(dat$trait))] +
                rnorm(nrow(dat), sd = sig_eps)
  dat$obs <- factor(paste(dat$individual, dat$session, sep = "_"))
  .glllvm_with_keywords(quote(
    gllvmTMB::gllvmTMB(
      value ~ 0 + trait +
              latent(0 + trait | individual, d = 2) +
              `unique`(0 + trait | individual) +
              latent(0 + trait | obs, d = 1) +
              `unique`(0 + trait | obs),
      data     = dat,
      family   = stats::gaussian(),
      trait    = "trait",
      unit     = "individual",
      unit_obs = "obs",
      cluster  = "session",
      silent   = TRUE
    )
  ))
}
```

- [ ] **Step 2: Run the Task 1 tests again.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-symbolize-gllvmtmb-two-tier.R", reporter = "summary")'`
Expected: both tests pass — `glm_has_within_unit(fit_gllvm_two_tier())` returns TRUE; `glm_has_within_unit(fit_gllvm_with_unique())` returns FALSE.

- [ ] **Step 3: Commit.**

```bash
git add tests/testthat/helper-gllvm-fits.R
git commit -m "test(gllvm): add fit_gllvm_two_tier() fixture for Σ_B + Σ_W tests"
```

---

## Task 3: Extractor — populate `expanded$Lambda_W`, `Z_W`, `Psi_W`

**Files:**
- Modify: `R/symbolize-gllvmtmb.R` — extend `glm_build_expanded()`
- Test: `tests/testthat/test-symbolize-gllvmtmb-two-tier.R`

- [ ] **Step 1: Add the failing tests.** Append to `test-symbolize-gllvmtmb-two-tier.R`:

```r
test_that("expanded$Lambda_W is populated for a two-tier fit (n_traits x d_W)", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  expect_true(!is.null(sym$expanded$Lambda_W),
              info = "expanded$Lambda_W is NULL on a two-tier fit")
  expect_true(is.matrix(sym$expanded$Lambda_W))
  expect_equal(nrow(sym$expanded$Lambda_W), as.integer(fit$n_traits))
  # d_W stored in tmb_data
  expect_equal(ncol(sym$expanded$Lambda_W),
               as.integer(fit$tmb_data$d_W))
})

test_that("expanded$Z_W is the n_obs x d_W observation-level latent matrix", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  expect_true(!is.null(sym$expanded$Z_W))
  expect_true(is.matrix(sym$expanded$Z_W))
  expect_equal(nrow(sym$expanded$Z_W), length(sym$expanded$y))
  expect_equal(ncol(sym$expanded$Z_W),
               as.integer(fit$tmb_data$d_W))
})

test_that("expanded$Psi_W is a length-n_traits SD vector on a two-tier fit", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  expect_true(!is.null(sym$expanded$Psi_W))
  expect_true(is.numeric(sym$expanded$Psi_W))
  expect_equal(length(sym$expanded$Psi_W), as.integer(fit$n_traits))
})

test_that("expanded$Lambda_W / Z_W / Psi_W stay NULL on a between-only fit", {
  fit <- fit_gllvm_with_unique()  # between only
  sym <- symbolize(fit)
  expect_null(sym$expanded$Lambda_W)
  expect_null(sym$expanded$Z_W)
  expect_null(sym$expanded$Psi_W)
})
```

- [ ] **Step 2: Run RED.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-symbolize-gllvmtmb-two-tier.R", reporter = "summary")'`
Expected: 4 failures (all four new tests; the two prior tests still pass).

- [ ] **Step 3: Extend `glm_build_expanded()`.** Open `R/symbolize-gllvmtmb.R` and locate the existing extractor (around the `Lambda_B`/`Sigma_B`/`Z_B`/`Psi_B` block inside `glm_build_expanded()`). After the `Psi_B` line, add:

```r
  # Within-unit (obs-level) tier — populated only when the fit carries
  # rr() and/or diag() covstructs whose grouping factor differs from
  # the unit. gllvmTMB exposes them via `fit$report$Lambda_W` and
  # `sd_W`; the per-obs latent scores Z_W via `gllvmTMB::getLV(level=
  # "unit_obs")`.
  Lambda_W <- if (isTRUE(glm_has_within_unit(fit)) &&
                  !is.null(fit$report$Lambda_W))
    as.matrix(fit$report$Lambda_W) else NULL
  Z_W <- if (isTRUE(glm_has_within_unit(fit)))
    tryCatch(as.matrix(gllvmTMB::getLV(fit, level = "unit_obs")),
             error = function(...) NULL) else NULL
  Psi_W <- if (isTRUE(glm_has_within_unit(fit)) &&
               !is.null(fit$report$sd_W))
    as.numeric(fit$report$sd_W) else NULL
```

Then in the return list, after the existing `Psi_B = Psi_B,` line, add three new fields:

```r
    Lambda_W  = Lambda_W,
    Z_W       = Z_W,
    Psi_W     = Psi_W,
```

- [ ] **Step 4: Run GREEN.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-symbolize-gllvmtmb-two-tier.R", reporter = "summary")'`
Expected: all four new tests pass; the regression test on between-only fits (Lambda_W / Z_W / Psi_W are NULL) also passes.

- [ ] **Step 5: Commit.**

```bash
git add R/symbolize-gllvmtmb.R tests/testthat/test-symbolize-gllvmtmb-two-tier.R
git commit -m "feat(gllvm): expanded carries Lambda_W, Z_W, Psi_W on two-tier fits"
```

---

## Task 4: Extractor — populate `expanded$Sigma_B` and `expanded$Sigma_W`

`Sigma_B` may already be present from prior slice — confirm and extend if needed. `Sigma_W` is new and is the algebraic closure `Λ_W Λ_W^T + diag(Ψ_W²)`.

**Files:**
- Modify: `R/symbolize-gllvmtmb.R`
- Test: `tests/testthat/test-symbolize-gllvmtmb-two-tier.R`

- [ ] **Step 1: Add failing tests.** Append:

```r
test_that("expanded$Sigma_W closes Λ_W Λ_W^T + diag(Ψ_W²) to 1e-6", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  expect_true(!is.null(sym$expanded$Sigma_W))
  expect_true(is.matrix(sym$expanded$Sigma_W))
  T_n <- as.integer(fit$n_traits)
  expect_equal(dim(sym$expanded$Sigma_W), c(T_n, T_n))
  closure <- sym$expanded$Lambda_W %*% t(sym$expanded$Lambda_W) +
             diag(sym$expanded$Psi_W^2)
  expect_equal(sym$expanded$Sigma_W, closure, tolerance = 1e-6)
})

test_that("expanded$Sigma_B closes Λ_B Λ_B^T + diag(Ψ_B²) to 1e-6", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  expect_true(!is.null(sym$expanded$Sigma_B))
  closure <- sym$expanded$Lambda_B %*% t(sym$expanded$Lambda_B) +
             diag(sym$expanded$Psi_B^2)
  expect_equal(sym$expanded$Sigma_B, closure, tolerance = 1e-6)
})
```

- [ ] **Step 2: Run RED.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-symbolize-gllvmtmb-two-tier.R", reporter = "summary")'`
Expected: 2 new failures (Sigma_W NULL; Sigma_B may already exist but may not match closure if Psi_B was previously dropped).

- [ ] **Step 3: Extend the extractor.** In `glm_build_expanded()`, immediately after the Λ_W / Z_W / Ψ_W block from Task 3, add:

```r
  Sigma_W <- if (!is.null(Lambda_W) && !is.null(Psi_W))
    Lambda_W %*% t(Lambda_W) + diag(Psi_W^2) else NULL
```

For `Sigma_B`: locate the existing line that sets `Sigma_B <- ...` from `fit$report$Sigma_B`. Replace it with:

```r
  Sigma_B <- if (!is.null(fit$report$Sigma_B))
    as.matrix(fit$report$Sigma_B)
  else if (!is.null(Lambda_B) && !is.null(Psi_B))
    Lambda_B %*% t(Lambda_B) + diag(Psi_B^2)
  else NULL
```

Then in the return list, after `Psi_W = Psi_W,`, add:

```r
    Sigma_W   = Sigma_W,
```

(`Sigma_B` is already in the return list from the prior slice — do not re-add it.)

- [ ] **Step 4: Run GREEN.**

Run the test command from Step 2.
Expected: both closure tests pass.

- [ ] **Step 5: Commit.**

```bash
git add R/symbolize-gllvmtmb.R tests/testthat/test-symbolize-gllvmtmb-two-tier.R
git commit -m "feat(gllvm): expanded carries Sigma_W = Λ_W Λ_W^T + Ψ_W² and reasserts Sigma_B closure"
```

---

## Task 5: Extractor helper — `glm_compute_repeatability(Sigma_B, Sigma_W)`

Per-trait repeatability `R_t = (Σ_B)_tt / [(Σ_B)_tt + (Σ_W)_tt]` per Eq 25 of the Nakagawa paper.

**Files:**
- Modify: `R/symbolize-gllvmtmb.R`
- Test: `tests/testthat/test-symbolize-gllvmtmb-two-tier.R`

- [ ] **Step 1: Add failing test.**

```r
test_that("glm_compute_repeatability returns the per-trait R_t formula", {
  Sigma_B <- diag(c(0.62, 0.45, 0.58, 0.31, 0.40))
  Sigma_W <- diag(c(0.38, 0.55, 0.42, 0.69, 0.60))
  rep_v   <- symbolizer:::glm_compute_repeatability(Sigma_B, Sigma_W)
  expect_equal(rep_v, diag(Sigma_B) / (diag(Sigma_B) + diag(Sigma_W)),
               tolerance = 1e-12)
  expect_equal(length(rep_v), 5L)
})

test_that("glm_compute_repeatability returns NULL when Sigma_W is NULL", {
  Sigma_B <- diag(c(0.62, 0.45, 0.58))
  expect_null(symbolizer:::glm_compute_repeatability(Sigma_B, NULL))
})
```

- [ ] **Step 2: Run RED.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-symbolize-gllvmtmb-two-tier.R", reporter = "summary")'`
Expected: 2 failures (function undefined).

- [ ] **Step 3: Add the helper to `R/symbolize-gllvmtmb.R`** right after `glm_has_within_unit()`:

```r
glm_compute_repeatability <- function(Sigma_B, Sigma_W) {
  if (is.null(Sigma_W) || is.null(Sigma_B)) return(NULL)
  diag(Sigma_B) / (diag(Sigma_B) + diag(Sigma_W))
}
```

- [ ] **Step 4: Run GREEN.**

Same command as Step 2.
Expected: both new tests pass.

- [ ] **Step 5: Commit.**

```bash
git add R/symbolize-gllvmtmb.R tests/testthat/test-symbolize-gllvmtmb-two-tier.R
git commit -m "feat(gllvm): add glm_compute_repeatability() (per-trait R_t)"
```

---

## Task 6: Extractor — populate `expanded$Repeatability`

**Files:**
- Modify: `R/symbolize-gllvmtmb.R`
- Test: `tests/testthat/test-symbolize-gllvmtmb-two-tier.R`

- [ ] **Step 1: Add failing test.**

```r
test_that("expanded$Repeatability is the per-trait R_t vector on a two-tier fit", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  expect_true(!is.null(sym$expanded$Repeatability))
  expect_equal(length(sym$expanded$Repeatability),
               as.integer(fit$n_traits))
  # Must equal the explicit formula
  expected_R <- diag(sym$expanded$Sigma_B) /
                (diag(sym$expanded$Sigma_B) + diag(sym$expanded$Sigma_W))
  expect_equal(sym$expanded$Repeatability, expected_R, tolerance = 1e-12)
})

test_that("expanded$Repeatability stays NULL on a between-only fit", {
  fit <- fit_gllvm_with_unique()
  sym <- symbolize(fit)
  expect_null(sym$expanded$Repeatability)
})
```

- [ ] **Step 2: Run RED.**

Same command. Expected: 2 failures.

- [ ] **Step 3: Extend `glm_build_expanded()`.** After the `Sigma_W <- ...` line from Task 4:

```r
  Repeatability <- glm_compute_repeatability(Sigma_B, Sigma_W)
```

And add to the return list:

```r
    Repeatability = Repeatability,
```

- [ ] **Step 4: Run GREEN.** Expected: both pass.

- [ ] **Step 5: Commit.**

```bash
git add R/symbolize-gllvmtmb.R tests/testthat/test-symbolize-gllvmtmb-two-tier.R
git commit -m "feat(gllvm): expanded carries Repeatability (per-trait R_t)"
```

---

## Task 7: Verify full gllvm test suite still green

**Files:** none new — sanity gate before moving to the renderer.

- [ ] **Step 1: Run all gllvm-related tests.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_dir("tests/testthat", filter = "gllvm", reporter = "summary")'`
Expected: all tests pass (the new two-tier file + the existing `test-symbolize-gllvmtmb-expanded.R` + `test-symbolize-gllvmtmb.R` + `test-symbolize-gllvmtmb-binomial.R`).

- [ ] **Step 2: If anything regressed**, stop and debug before moving on. Likely culprits: the change to `Sigma_B` computation in Task 4 — confirm the pre-existing `Sigma_B` assertion in `test-symbolize-gllvmtmb.R:170` still matches.

---

## Task 8: Renderer helper — `latex_implied_cov_block(name, Sigma, Lambda, Psi, fmt)`

Emits the LaTeX for one tier's implied-covariance decomposition: numerical Σ matrix shown next to its `Λ Λ^T + diag(Ψ²)` decomposition.

**Files:**
- Modify: `R/render-three-views.R`
- Test: `tests/testthat/test-render-implied-cov.R` (create)

- [ ] **Step 1: Create the test file with a failing test.**

```r
# Tests for the latex_implied_cov_block emitter used in Tab 3 of the
# gllvm widget. Each tier (B and W) emits a stacked block that shows
# the numerical Σ next to Λ Λ^T + diag(Ψ²).

test_that("latex_implied_cov_block emits the three-term decomposition", {
  Lambda <- matrix(c(1.0, 0.5, 0.2,
                     0.0, 0.7, 0.3), nrow = 3, byrow = FALSE)
  Psi    <- c(0.4, 0.3, 0.5)
  Sigma  <- Lambda %*% t(Lambda) + diag(Psi^2)
  out <- symbolizer:::latex_implied_cov_block(
    tier = "B", Sigma = Sigma, Lambda = Lambda, Psi = Psi
  )
  # Returns a single character string containing the implied-cov LaTeX block.
  expect_type(out, "character")
  expect_length(out, 1L)
  # Contains the three matrix labels
  expect_match(out, "\\\\boldsymbol\\{\\\\Sigma\\}_B", fixed = FALSE)
  expect_match(out, "\\\\boldsymbol\\{\\\\Lambda\\}_B", fixed = FALSE)
  expect_match(out, "\\\\boldsymbol\\{\\\\Psi\\}_B", fixed = FALSE)
  # Caption identifies the tier
  expect_match(out, "between-individual", fixed = TRUE)
})

test_that("latex_implied_cov_block returns NULL when any matrix is NULL", {
  expect_null(symbolizer:::latex_implied_cov_block(
    tier = "W", Sigma = NULL, Lambda = NULL, Psi = NULL
  ))
})

test_that("latex_implied_cov_block tier = 'W' uses Λ_W / Ψ_W / Σ_W labels", {
  Lambda <- matrix(0.3, nrow = 3, ncol = 1)
  Psi    <- c(0.5, 0.4, 0.6)
  Sigma  <- Lambda %*% t(Lambda) + diag(Psi^2)
  out <- symbolizer:::latex_implied_cov_block(
    tier = "W", Sigma = Sigma, Lambda = Lambda, Psi = Psi
  )
  expect_match(out, "\\\\boldsymbol\\{\\\\Sigma\\}_W", fixed = FALSE)
  expect_match(out, "\\\\boldsymbol\\{\\\\Lambda\\}_W", fixed = FALSE)
  expect_match(out, "\\\\boldsymbol\\{\\\\Psi\\}_W", fixed = FALSE)
  expect_match(out, "within-individual", fixed = TRUE)
})
```

- [ ] **Step 2: Run RED.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-render-implied-cov.R", reporter = "summary")'`
Expected: 3 failures (function undefined).

- [ ] **Step 3: Add the emitter to `R/render-three-views.R`.** Place near the existing `latex_mat()` / `latex_vec()` helpers (search for `latex_mat <- function` to locate). Insert:

```r
# Emit the implied-covariance block for one tier (B or W) of a gllvm
# fit: a stacked equation showing the numerical Σ matrix next to its
# Λ Λ^T + diag(Ψ²) decomposition. Returns a character string suitable
# for inclusion inside a `$$ ... $$` MathJax block, or NULL when any
# matrix is missing.
latex_implied_cov_block <- function(tier = c("B", "W"), Sigma, Lambda, Psi) {
  tier <- match.arg(tier)
  if (is.null(Sigma) || is.null(Lambda) || is.null(Psi)) return(NULL)
  caption <- if (identical(tier, "B"))
    "between-individual implied covariance"
  else
    "within-individual implied covariance"
  T_n <- nrow(Sigma); d <- ncol(Lambda)
  sigma_mat <- latex_mat(Sigma, rows = seq_len(min(T_n, 7L)))
  lam_mat   <- latex_mat(Lambda, rows = seq_len(min(T_n, 7L)))
  psi_mat   <- latex_mat(diag(Psi^2), rows = seq_len(min(T_n, 7L)))
  sigma_lab <- sprintf("\\boldsymbol{\\Sigma}_%s\\;\\text{(%s)}",
                       tier, caption)
  lam_lab   <- sprintf("\\boldsymbol{\\Lambda}_%s\\,\\boldsymbol{\\Lambda}_%s^{\\!\\top}",
                       tier, tier)
  psi_lab   <- sprintf("\\boldsymbol{\\Psi}_%s^{\\,2}", tier)
  paste0(
    "$$\n",
    underbrace(sigma_mat, sigma_lab),
    " \\;=\\; ", underbrace(lam_mat, lam_lab),
    " \\;+\\; ", underbrace(psi_mat, psi_lab),
    "\n$$\n"
  )
}
```

If `underbrace()` isn't already a helper, locate the existing one in `render-three-views.R` (search `underbrace`). It exists from prior slices. If for some reason it doesn't, define it inline:

```r
underbrace <- function(content, label)
  sprintf("\\underbrace{%s}_{\\textstyle\\,%s\\,}", content, label)
```

- [ ] **Step 4: Run GREEN.**

Same command. Expected: all 3 tests pass.

- [ ] **Step 5: Commit.**

```bash
git add R/render-three-views.R tests/testthat/test-render-implied-cov.R
git commit -m "feat(render): latex_implied_cov_block emits Σ = ΛΛᵀ + diag(Ψ²) per tier"
```

---

## Task 9: Renderer — Tab 3 conditionally appends implied-cov blocks

Tab 3 (panel-mat) gets the new implied-cov blocks after the existing stacked-equation block. Widget 1 (between only) gets one B-tier block; Widget 2 (two tiers) gets B then W blocks plus a per-trait repeatability row.

**Files:**
- Modify: `R/render-three-views.R` — extend `three_views_eq_block_data()` (or whichever function emits panel-mat content)

- [ ] **Step 1: Add a failing renderer-integration test.** Create `tests/testthat/test-three-views-implied-cov.R`:

```r
# Verifies that as_html_three_views() emits the implied-cov block when
# expanded$Sigma_B is present, AND the second block (W) plus repeatability
# row when expanded$Sigma_W is present.

test_that("Tab 3 emits a B-tier implied-cov block on a between-only gllvm fit", {
  fit <- fit_gllvm_with_unique()  # between only
  sym <- symbolize(fit)
  html <- as_html_three_views(sym, id = "test-B")
  # Σ_B label appears (with the between-individual caption)
  expect_match(html, "between-individual implied covariance", fixed = TRUE)
  # Σ_W label does NOT appear
  expect_false(grepl("within-individual implied covariance", html, fixed = TRUE))
})

test_that("Tab 3 emits B + W blocks and Repeatability row on a two-tier fit", {
  fit <- fit_gllvm_two_tier()
  sym <- symbolize(fit)
  html <- as_html_three_views(sym, id = "test-BW")
  expect_match(html, "between-individual implied covariance", fixed = TRUE)
  expect_match(html, "within-individual implied covariance", fixed = TRUE)
  # Per-trait repeatability row
  expect_match(html, "Per-trait repeatability", fixed = TRUE)
  # All five R_t values referenced (formatted to 3 sig figs)
  R_vec <- sym$expanded$Repeatability
  for (k in seq_along(R_vec)) {
    expect_match(html, formatC(R_vec[k], digits = 3, format = "g"),
                 fixed = TRUE)
  }
})
```

- [ ] **Step 2: Run RED.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-three-views-implied-cov.R", reporter = "summary")'`
Expected: both fail — current renderer doesn't emit either block.

- [ ] **Step 3: Extend the Tab-3 emitter.** Open `R/render-three-views.R`, find `three_views_eq_block_data()` (the function that emits panel-mat content). Locate the closing `</div>` for the stacked-block emitter. Before that closing tag, insert:

```r
  # Implied-covariance blocks (gllvm two-tier widget). For Widget 1
  # (between only) emit just the B-tier block; for Widget 2 (two-tier)
  # emit B then W, plus a per-trait repeatability row.
  sigma_B_block <- latex_implied_cov_block(
    tier = "B",
    Sigma = ex$Sigma_B, Lambda = ex$Lambda_B, Psi = ex$Psi_B
  )
  sigma_W_block <- latex_implied_cov_block(
    tier = "W",
    Sigma = ex$Sigma_W, Lambda = ex$Lambda_W, Psi = ex$Psi_W
  )
  if (!is.null(sigma_B_block)) {
    out <- paste0(out,
      "<p class=\"sym-caption\" style=\"font-size:0.95em;color:#374151\">",
      "Implied between-individual trait covariance ",
      "$\\boldsymbol{\\Sigma}_B$ ",
      "decomposes into a shared low-rank part and per-trait uniquenesses:",
      "</p>\n<div class=\"sym-eq\">", sigma_B_block, "</div>\n")
  }
  if (!is.null(sigma_W_block)) {
    out <- paste0(out,
      "<p class=\"sym-caption\" style=\"font-size:0.95em;color:#374151\">",
      "Implied within-individual trait covariance ",
      "$\\boldsymbol{\\Sigma}_W$ ",
      "(replaces the $\\sigma^2_\\epsilon$ row-level residual in this fit):",
      "</p>\n<div class=\"sym-eq\">", sigma_W_block, "</div>\n")
    # Per-trait repeatability row when both tiers present.
    if (!is.null(ex$Repeatability)) {
      R_vec <- ex$Repeatability
      R_txt <- paste(vapply(R_vec, function(v)
        formatC(v, digits = 3, format = "g"), character(1L)),
        collapse = ", ")
      out <- paste0(out,
        "<p class=\"sym-caption\" style=\"font-size:0.95em;color:#374151\">",
        "Per-trait repeatability $R_t = (\\Sigma_B)_{tt} / ",
        "[(\\Sigma_B)_{tt} + (\\Sigma_W)_{tt}]$: $[",
        R_txt,
        "]$. Each $R_t$ is the share of trait $t$'s total variance that ",
        "lives at the <em>between-individual</em> tier.",
        "</p>\n")
    }
  }
```

(Adapt `out <- paste0(out, ...)` to whatever variable name the existing emitter uses for the running output string.)

- [ ] **Step 4: Run GREEN.**

Run the same test command from Step 2.
Expected: both pass.

- [ ] **Step 5: Run full renderer suite for regressions.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_dir("tests/testthat", filter = "render|three-views", reporter = "summary")'`
Expected: all green.

- [ ] **Step 6: Commit.**

```bash
git add R/render-three-views.R tests/testthat/test-three-views-implied-cov.R
git commit -m "feat(render): Tab 3 emits Σ_B + Σ_W implied-cov blocks and per-trait R_t row"
```

---

## Task 10: Sanity — run the full gllvm + renderer suite

- [ ] **Step 1: Run all gllvm + render tests.**

Run: `Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_dir("tests/testthat", filter = "gllvm|render|three-views", reporter = "summary")'`
Expected: all pass; no regression on the existing tests.

- [ ] **Step 2: If anything regressed**, stop and debug. The pre-existing `Sigma_B` regression test at `test-symbolize-gllvmtmb.R:170` (the `Matrix::expand` masking issue from issue #7) is a known pre-existing failure — confirm it's not a new failure introduced by today's changes.

---

## Task 11: CSV — `inst/extdata/capabilities.csv`

Add capability rows for the new slots.

**Files:**
- Modify: `inst/extdata/capabilities.csv`

- [ ] **Step 1: Open the file** and locate the existing `gllvmTMB, gaussian, Sigma_B` line.

- [ ] **Step 2: Add four new rows** below the existing `gllvmTMB, gaussian, Psi_B` row:

```csv
gllvmTMB,gaussian,Lambda_W,First slice,0.21.6,"Within-individual reduced-rank loadings via latent(0 + trait | obs, d = d_W)."
gllvmTMB,gaussian,Sigma_W,First slice,0.21.6,"Implied within-individual covariance Lambda_W Lambda_W^T (+ Psi_W when unique(0 + trait | obs) present)."
gllvmTMB,gaussian,Psi_W,First slice,0.21.6,"Per-trait within-individual unique variance via unique(0 + trait | obs)."
gllvmTMB,gaussian,Repeatability,First slice,0.21.6,"Per-trait repeatability R_t = (Sigma_B)_tt / [(Sigma_B)_tt + (Sigma_W)_tt]."
```

- [ ] **Step 3: Verify the CSV parses.**

Run: `Rscript -e 'd <- read.csv("inst/extdata/capabilities.csv"); stopifnot(nrow(d) >= 49); cat("OK,", nrow(d), "rows\n")'`
Expected: `OK, 49 rows` (or higher).

- [ ] **Step 4: Commit.**

```bash
git add inst/extdata/capabilities.csv
git commit -m "data(capabilities): add gllvmTMB Lambda_W / Sigma_W / Psi_W / Repeatability rows"
```

---

## Task 12: CSV — `inst/extdata/assumption-templates.csv`

Add a Σ_W decomposition row for `gllvm_gaussian` mirroring the existing Σ_B row.

**Files:**
- Modify: `inst/extdata/assumption-templates.csv`

- [ ] **Step 1: Locate the existing line** containing `Sigma_B,covariance_decomposition,"\boldsymbol{\Sigma}_B = ...`.

- [ ] **Step 2: Add a new row immediately below it:**

```csv
gllvm_gaussian,Sigma_W,covariance_decomposition,"\boldsymbol{\Sigma}_W = \boldsymbol{\Lambda}_W \boldsymbol{\Lambda}_W^\top + \boldsymbol{\Psi}_W","The implied within-individual trait covariance decomposes into a shared low-rank part (Lambda_W Lambda_W^T) and trait-specific within-individual unique variances (Psi_W). Replaces the sigma_epsilon^2 I_T row-level residual when unique(0 + trait | obs) is present (gllvmTMB auto-suppresses sigma_eps in that case).",stated,
```

- [ ] **Step 3: Verify the CSV parses + tally rows.**

Run: `Rscript -e 'd <- read.csv("inst/extdata/assumption-templates.csv"); cat("OK,", nrow(d), "rows\n"); print(head(d[d$family == "gllvm_gaussian", c("submodel", "kind")]))'`
Expected: `OK, N rows` (N = previous + 1); the gllvm_gaussian rows include both `Sigma_B` and `Sigma_W` covariance_decomposition rows.

- [ ] **Step 4: Commit.**

```bash
git add inst/extdata/assumption-templates.csv
git commit -m "data(assumptions): add Σ_W = Λ_W Λ_W^T + Ψ_W decomposition row for gllvm_gaussian"
```

---

## Task 13: CSV — `inst/extdata/family-parameterizations.csv`

Update the gllvm_gaussian description to mention the two-tier Σ decomposition.

**Files:**
- Modify: `inst/extdata/family-parameterizations.csv`

- [ ] **Step 1: Locate the `gllvm_gaussian` row.**

- [ ] **Step 2: Update the description column** to mention both tiers. Replace the existing free-text description with:

```
"gllvmTMB Gaussian latent-variable model: each row is a (unit, trait) cell; the conditional mean carries trait intercepts plus a reduced-rank between-unit latent contribution (Λ_B z_{B,i})_t; the implied between-unit trait covariance is Sigma_B = Lambda_B Lambda_B^T + Psi_B. When the fit adds an obs-level latent() + unique() pair, the model becomes two-tier: Sigma_W = Lambda_W Lambda_W^T + Psi_W replaces the row-level sigma_eps^2 I_T residual. The trait-specific repeatability R_t = (Sigma_B)_tt / [(Sigma_B)_tt + (Sigma_W)_tt] is the natural integration-vs-plasticity readout."
```

- [ ] **Step 3: Verify the CSV parses.**

Run: `Rscript -e 'd <- read.csv("inst/extdata/family-parameterizations.csv"); g <- d[d$family == "gllvm_gaussian", ]; cat("OK; description length:", nchar(g$description), "\n")'`
Expected: prints the description length (should be >= 600 chars).

- [ ] **Step 4: Commit.**

```bash
git add inst/extdata/family-parameterizations.csv
git commit -m "data(family-params): gllvm_gaussian now describes the two-tier Σ_B + Σ_W story"
```

---

## Task 14: Article — §3 *Univariate motivation* (NEW)

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Open the file** and locate the existing `## 4. The fit` heading.

- [ ] **Step 2: Above it, insert a new §3 between §2 (data) and the existing §4.** Renumber the existing §3, §4, ... by 1.

Insert before what is currently `## 3. The model in symbols` (which becomes §5):

```markdown
## 3. The univariate root: one trait per row

Before we model five traits jointly, recall the univariate mixed model
behavioural ecologists already know. For one trait $y$ measured on $N$
individuals at $J_i$ occasions:

$$
y_{ij} = \mu + u_i + e_{ij}, \qquad
u_i \sim \mathcal{N}(0, \sigma^2_u), \qquad
e_{ij} \sim \mathcal{N}(0, \sigma^2_e).
$$

The classical repeatability (Nakagawa & Schielzeth 2010) is

$$
R = \frac{\sigma^2_u}{\sigma^2_u + \sigma^2_e}.
$$

In `lme4` syntax:

```{r univariate}
library(lme4)
# Single-trait fit (illustrative; uses the first trait of the simulated set)
fit_uni <- lmer(value ~ 1 + (1 | individual),
                data = subset(dat, trait == "t1"))
summary(fit_uni)$varcor
```

That `(1 | individual)` random intercept captures stable between-individual
variation; everything else lands in $\sigma^2_e$. When we move to many
traits jointly, we want the *multivariate* version of this decomposition —
which traits move together between individuals, which traits move together
within individuals — and the GLLVM is the parsimonious way to write it.

**Takeaway.** Repeatability $R$ is between-individual share of total
variance, computed trait-by-trait. The GLLVM upgrade replaces the two
scalars $\sigma^2_u, \sigma^2_e$ with two $T \times T$ matrices
$\boldsymbol{\Sigma}_B, \boldsymbol{\Sigma}_W$ and decomposes each.

```

- [ ] **Step 3: Renumber subsequent sections** (the existing `## 3. The model in symbols` becomes `## 5.`, existing `## 4. The fit` becomes `## 6.`, and so on — bump every `## N.` by 2 from §3 onward). Use a single sed pass to be safe:

```bash
# Manual edit only — sed across the whole file would mis-renumber chunk
# headings. Read the file, change every "## 3.", "## 4.", ... to "## 5.",
# "## 6.", ... in order, by hand.
```

(Do this carefully — do not use a global regex; section numbers must stay
unique and sequential.)

- [ ] **Step 4: Build the article.**

Run: `cd "/Users/z3437171/Dropbox/Github Local/symbolizer-hotfix" && Rscript -e 'suppressMessages({ devtools::install(quiet = TRUE, upgrade = "never"); pkgdown::build_article("symbolizer-gllvm", quiet = TRUE) })' && echo "BUILD OK"`
Expected: `BUILD OK`.

- [ ] **Step 5: Verify §3 renders.**

Run: `grep -c "univariate root" docs/articles/symbolizer-gllvm.html`
Expected: 2 (heading + anchor).

- [ ] **Step 6: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): add §3 univariate motivation (Nakagawa-style build-up)"
```

---

## Task 15: Article — §4 *Why factor-analytic* (NEW)

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Insert** between the new §3 and the renumbered §5:

```markdown
## 4. Why factor-analytic? The curse-of-dimensionality argument

A fully parameterised multivariate covariance for $T = 5$ traits at one
tier requires $T(T+1)/2 = 15$ free parameters. For two tiers (between +
within) that's $30$, and at $T = 10$ traits the count balloons to $110$
covariance parameters before any fixed effects enter. Animal-personality
studies typically have $N \approx 40$–$200$ individuals; the resulting
likelihood is *flat* — convergence is unstable and even when fits return,
the standard errors are wide enough that interpretation becomes risky.

The **factor-analytic** answer is to decompose

$$
\boldsymbol{\Sigma}_B = \boldsymbol{\Lambda}_B \boldsymbol{\Lambda}_B^{\!\top} + \boldsymbol{\Psi}_B,
$$

with $\boldsymbol{\Lambda}_B \in \mathbb{R}^{T \times d_B}$ a reduced-rank
loading matrix ($d_B \ll T$) and $\boldsymbol{\Psi}_B$ a diagonal of
trait-specific uniquenesses. The parameter count drops from $T(T+1)/2$ to

$$
T(d_B + 1) - d_B(d_B - 1)/2,
$$

so for $T = 5, d_B = 2$: $15 \to 14$; for $T = 10, d_B = 2$: $55 \to 29$;
for $T = 10, d_B = d_W = 2$ at both tiers: $110 \to 58$. The reduction is
modest when $T$ is small and dramatic as $T$ grows.

The *interpretive* payoff is the same shape both directions. The shared
loadings $\boldsymbol{\Lambda}_B$ name the between-individual axes — what
the behavioural-syndromes literature calls "personality axes" or just
*syndromes*. The uniquenesses $\boldsymbol{\Psi}_B$ name the leftover —
the trait-specific between-individual variance the shared axes do not
explain. We will see in §6 that biologists can read both from a single
fitted matrix.

**Takeaway.** Reduced-rank decomposition $\boldsymbol{\Sigma} = \boldsymbol{\Lambda} \boldsymbol{\Lambda}^{\!\top} + \boldsymbol{\Psi}$ both stabilises the fit (parameter count drops) and surfaces the biology (loadings *are* the syndromes).
```

- [ ] **Step 2: Renumber** the rest of the article by +1 (so what was §5 *The model in symbols* becomes §6 — but actually that's already shifted from Task 14; the cumulative renumbering is: original §3→§5, §4→§6, ... §9→§11). Do the renumbering for §5 onward NOW so the sections form a clean 1–11 sequence.

- [ ] **Step 3: Build + verify.**

Run: `Rscript -e 'suppressMessages({ devtools::install(quiet = TRUE, upgrade = "never"); pkgdown::build_article("symbolizer-gllvm", quiet = TRUE) })' && grep -c "curse-of-dimensionality" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 1.

- [ ] **Step 4: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): add §4 'why factor-analytic' parameter-counting argument"
```

---

## Task 16: Article — §5 *The model in symbols* (light edits)

The existing long-form / wide-form side-by-side stays, but adopt **Ψ-naming** consistently and reference the just-introduced reduced-rank motivation from §4.

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Find the long-form / wide-form chunk** (begins with `## 5. The model in symbols` after the renumbering).

- [ ] **Step 2: Add one transition paragraph** at the top:

```markdown
Building on §4: the same model can be written two ways. The **long form**
matches the way `gllvmTMB()` actually fits the data — one row per
(individual, occasion, trait) cell, with a scalar response. The **wide
form** matches the factor-analytic textbooks — one row per observation,
with a trait-vector response. The two panels below place them side by
side; both decompose covariance via $\boldsymbol{\Sigma} = \boldsymbol{\Lambda}\boldsymbol{\Lambda}^{\!\top} + \boldsymbol{\Psi}$ at each
tier.
```

- [ ] **Step 3: Audit the chunk for Ψ-naming.** Replace any leftover `s^2` / `S_B` / `S_W` notation with `\boldsymbol{\Psi}_B` / `\boldsymbol{\Psi}_W`. Keep `\sigma^2_{e}` (the within-individual row-level residual variance) in the long form for §5; that's the scalar that gets auto-suppressed only when the within-tier `unique()` is added in Widget 2.

- [ ] **Step 4: Build + diff.**

Run: `Rscript -e 'pkgdown::build_article("symbolizer-gllvm", quiet = TRUE)' && grep -c "boldsymbol{\\\\Psi}" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 4.

- [ ] **Step 5: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §5 light edits — Ψ-naming throughout, link to §4 motivation"
```

---

## Task 17: Article — §6 *Widget 1 — Syndromes* (rewrite)

Replace the previous two-widget split (between-only without Ψ_B, between-only with Ψ_B) with a single Widget 1: `Σ_B = Λ_B Λ_B^T + Ψ_B`. This is the **real syndromes model** — between-individual reduced-rank loadings *plus* trait-specific between-individual uniquenesses.

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Delete the current §6 + §7 widget block** (the two widgets from v0.21.5-redo). They are superseded.

- [ ] **Step 2: In its place, insert §6:**

```markdown
## 6. Widget 1 — Behavioural syndromes

The first fit decomposes the **between-individual** trait covariance only.
Each trait gets its own grand mean; the shared between-individual
structure is captured by a rank-$d_B$ loading matrix $\boldsymbol{\Lambda}_B$ plus per-trait
uniquenesses $\boldsymbol{\Psi}_B$:

$$
\boldsymbol{\Sigma}_B = \boldsymbol{\Lambda}_B \boldsymbol{\Lambda}_B^{\!\top} + \boldsymbol{\Psi}_B.
$$

The within-individual residual is the shared row-level Gaussian $\sigma_\epsilon^2$.

```{r fit-syndromes}
fit_syndromes <- gllvmTMB(
  value ~ 0 + trait +
          latent(0 + trait | individual, d = 2) +
          unique(0 + trait | individual),
  data     = dat,
  family   = gaussian(),
  trait    = "trait",
  unit     = "individual",
  silent   = TRUE
)
sym_syndromes <- symbolize(
  fit_syndromes,
  symbols = c(value = "y_{ij}", trait = "j", individual = "i"),
  context = "behavioural syndromes (between-individual only)"
)
```

### Three views — syndromes only

```{r widget-syndromes, results = 'asis', echo = FALSE, message = FALSE}
as_html_three_views(sym_syndromes, id = "syndromes")
```

The widget's **Tab 3 — Equations with data** carries the new
implied-covariance block: a 5 × 5 numerical $\boldsymbol{\Sigma}_B$
shown next to its decomposition $\boldsymbol{\Lambda}_B
\boldsymbol{\Lambda}_B^{\!\top} + \boldsymbol{\Psi}_B^{\,2}$. The arithmetic
closes element-by-element to within rounding.

**Takeaway.** Widget 1 is the canonical *behavioural syndromes* fit. The
two-axis $\boldsymbol{\Lambda}_B$ names the syndromes (which traits move
together between individuals); $\boldsymbol{\Psi}_B$ captures the leftover
per-trait between-individual variance.
```

- [ ] **Step 3: Build the article.**

Run: `Rscript -e 'suppressMessages({ devtools::install(quiet = TRUE, upgrade = "never"); pkgdown::build_article("symbolizer-gllvm", quiet = TRUE) })' && grep -c "Widget 1" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 1.

- [ ] **Step 4: Sanity-check on the live preview.**

Run: `curl -s "http://localhost:8767/articles/symbolizer-gllvm.html" | grep -c "behavioural syndromes"`
Expected: ≥ 1.

- [ ] **Step 5: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §6 Widget 1 — syndromes (Σ_B = Λ_B Λ_B^T + Ψ_B)"
```

---

## Task 18: Article — §7 *Widget 2 — Integrated plasticity* (rewrite)

The two-tier fit. Adds Λ_W + Ψ_W, triggers gllvmTMB's σ_ε auto-suppression, and Tab 3 emits BOTH B and W implied-cov blocks plus per-trait repeatability.

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Insert §7 immediately after §6:**

```markdown
## 7. Widget 2 — Adding integrated plasticity

The second fit adds the **within-individual** reduced-rank structure.
$\boldsymbol{\Lambda}_W$ captures shared within-individual axes (how traits
covary across occasions, within an individual); $\boldsymbol{\Psi}_W$
captures per-trait within-individual uniquenesses. Together:

$$
\boldsymbol{\Sigma}_W = \boldsymbol{\Lambda}_W \boldsymbol{\Lambda}_W^{\!\top} + \boldsymbol{\Psi}_W.
$$

When `unique(0 + trait | obs)` is added, gllvmTMB prints a notice:

> ℹ Auto-suppressing `sigma_eps`: `unique(0 + trait | obs)` is at the
> per-row level, so it already absorbs the observation residual.

This is intended — the row-level residual variance is now structured
per-trait in $\boldsymbol{\Psi}_W$ rather than carried by a single
scalar $\sigma_\epsilon^2$.

```{r fit-two-tier, message = FALSE, warning = FALSE}
fit_two_tier <- gllvmTMB(
  value ~ 0 + trait +
          latent(0 + trait | individual, d = 2) +
          unique(0 + trait | individual) +
          latent(0 + trait | obs, d = 1) +
          unique(0 + trait | obs),
  data     = dat,
  family   = gaussian(),
  trait    = "trait",
  unit     = "individual",
  unit_obs = "obs",
  cluster  = "session",
  silent   = TRUE
)
sym_two_tier <- symbolize(
  fit_two_tier,
  symbols = c(value = "y_{ij}", trait = "j", individual = "i"),
  context = "syndromes + integrated plasticity (two-tier)"
)
```

### Three views — two-tier

```{r widget-two-tier, results = 'asis', echo = FALSE, message = FALSE}
as_html_three_views(sym_two_tier, id = "two-tier")
```

What changes between Widget 1 and Widget 2:

- **Tab 1 (Index)**: the conditional mean adds $(\boldsymbol{\Lambda}_W \mathbf{z}_{W,ij})_t$. The conditional residual variance loses $\sigma_\epsilon^2$ and gains $\psi_{W,t}^2$ per trait.
- **Tab 2 (Matrix)**: the matrix-Normal carries both shared structures: $\boldsymbol{\eta} = \mathbf{1}_n \boldsymbol{\mu}^{\!\top} + \mathbf{Z}_B \boldsymbol{\Lambda}_B^{\!\top} + \mathbf{Z}_W \boldsymbol{\Lambda}_W^{\!\top}$, and the across-trait conditional covariance becomes $\boldsymbol{\Sigma}_W$ instead of $\sigma_\epsilon^2 \mathbf{I}_T$.
- **Tab 3 (Equations with data)**: gains a second implied-covariance block for $\boldsymbol{\Sigma}_W$, plus a per-trait repeatability row $R_t = (\boldsymbol{\Sigma}_B)_{tt} / [(\boldsymbol{\Sigma}_B)_{tt} + (\boldsymbol{\Sigma}_W)_{tt}]$.

**Takeaway.** Widget 2 carries the full two-tier model. The reader can
*see* the syndromes ($\boldsymbol{\Lambda}_B$, $\boldsymbol{\Sigma}_B$),
the integrated plasticity ($\boldsymbol{\Lambda}_W$,
$\boldsymbol{\Sigma}_W$), and the per-trait repeatability — all in one
widget, with arithmetic that closes.
```

- [ ] **Step 2: Build + verify the rendered article has both widgets.**

Run:
```bash
Rscript -e 'suppressMessages({ devtools::install(quiet = TRUE, upgrade = "never"); pkgdown::build_article("symbolizer-gllvm", quiet = TRUE) })'
grep -oE 'id="sym-(syndromes|twotier)-[0-9]+' docs/articles/symbolizer-gllvm.html | sort -u
```
Expected: both `sym-syndromes-...` and `sym-twotier-...` IDs appear.

- [ ] **Step 3: Verify both Σ_B and Σ_W blocks render on Widget 2's Tab 3.**

Run: `curl -s "http://localhost:8767/articles/symbolizer-gllvm.html" | grep -c "implied covariance"`
Expected: ≥ 3 (one B in Widget 1, B + W in Widget 2).

- [ ] **Step 4: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §7 Widget 2 — integrated plasticity (Σ_B + Σ_W)"
```

---

## Task 19: Article — §8 *Reading biologically* (rewrite)

Now reads communality $c^2_{B,t}$ + uniqueness $\psi_{B,t}$ at *both* tiers; repeatability $R_t$; phenotypic-correlation decomposition.

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Replace the existing §8 contents with:**

```markdown
## 8. Reading the latent axes biologically

Two payoffs once you have $\boldsymbol{\Lambda}_B, \boldsymbol{\Psi}_B,
\boldsymbol{\Lambda}_W, \boldsymbol{\Psi}_W$ in hand.

### 8.1 Communality and uniqueness at each tier

For trait $t$ at tier $g \in \{B, W\}$:

$$
c^2_{g,t} = \frac{\sum_{k} \lambda_{g,tk}^2}{(\boldsymbol{\Sigma}_g)_{tt}},
\qquad
\psi_{g,t} = \frac{(\boldsymbol{\Psi}_g)_{tt}}{(\boldsymbol{\Sigma}_g)_{tt}}.
$$

$c^2_{g,t}$ is the share of trait $t$'s total variance at tier $g$ that the shared axes explain;
$\psi_{g,t}$ is the leftover share — what's idiosyncratic to that trait at that tier.

```{r communality}
extract_communality(fit_two_tier)
```

A trait with high $c^2_B$ and high $c^2_W$ is *integrated at both tiers*:
both its between-individual position and its within-individual fluctuation
share the syndrome structure. A trait with high $c^2_B$ but low $c^2_W$ is
*syndromatic but not plastically integrated* — individuals' average levels
align, but their occasion-to-occasion fluctuations don't.

### 8.2 Repeatability and phenotypic-correlation decomposition

Per-trait repeatability already appears at the bottom of Widget 2's Tab 3:

$$
R_t = \frac{(\boldsymbol{\Sigma}_B)_{tt}}{(\boldsymbol{\Sigma}_B)_{tt} + (\boldsymbol{\Sigma}_W)_{tt}}.
$$

The phenotypic correlation between traits $t$ and $m$ decomposes as

$$
r_{P,tm} = r_{B,tm}\sqrt{R_t R_m} + r_{W,tm}\sqrt{(1 - R_t)(1 - R_m)},
$$

so the phenotypic-level correlation is a *weighted mix* of the
between-individual and within-individual correlations, weighted by how
much of each trait's variance lives at each tier (Dingemanse &
Dochtermann 2013, Eq 8). Symbolizer's `extract_correlations(fit)`
returns all three matrices ($\boldsymbol{C}_B, \boldsymbol{C}_W,
\boldsymbol{C}_P$) so a reader can verify this empirically:

```{r correlations}
extract_correlations(fit_two_tier)
```

**Takeaway.** $c^2$ and $\psi$ per tier are the trait-level integration
summaries; $R_t$ is the per-trait repeatability; phenotypic correlations
decompose as a weighted mix of between and within. All three readings
fall out of the same two fitted matrices.
```

- [ ] **Step 2: Build + verify.**

Run: `Rscript -e 'pkgdown::build_article("symbolizer-gllvm", quiet = TRUE)' && grep -c "phenotypic-correlation" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 1.

- [ ] **Step 3: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §8 reading — c²/ψ per tier, R_t, phenotypic-corr decomposition"
```

---

## Task 20: Article — §9 *Identifiability* (light edits + cross-reference to §5.3)

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Open §9** (currently *Identifiability gotchas: rotation and sign of Λ_B*). Append one paragraph linking to the spec's "What gllvmTMB silently does" content:

```markdown
A second category of identifiability concerns the **row-level residual
variance**: when both `latent(0 + trait | obs, ...)` and
`unique(0 + trait | obs)` are present (Widget 2), gllvmTMB auto-suppresses
$\sigma_\epsilon^2$ to keep the per-trait $\boldsymbol{\Psi}_W$ identifiable.
This is *not* a defect — it's the package's way of preventing a known
collinearity between two parametrisations of the same row-level variance.
Reading the auto-suppression message in the fit output is the expected
behaviour, not a warning to worry about.
```

- [ ] **Step 2: Build + verify.**

Run: `Rscript -e 'pkgdown::build_article("symbolizer-gllvm", quiet = TRUE)' && grep -c "auto-suppresses" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 1.

- [ ] **Step 3: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §9 — note σ_ε auto-suppression as expected behaviour, not a defect"
```

---

## Task 21: Article — §10 *glmmTMB bridge* (NEW)

Sidebar section showing the glmmTMB equivalence so readers from the Nakagawa paper aren't lost.

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Insert §10** between the renumbered §9 and §11:

```markdown
## 10. The glmmTMB bridge

`gllvmTMB` is purpose-built for generalised linear *latent-variable*
models. `glmmTMB` is a general-purpose GLMM package that, since
McGillycuddy et al. (2025), can fit the same math via the `rr()` and
`diag()` keywords. The Nakagawa et al. (in prep) paper uses the
`glmmTMB` route; for readers coming from there, the two-tier fit
translates one-to-one:

```{r glmmtmb-equivalence, eval = FALSE}
# gllvmTMB (article path)
fit_two_tier <- gllvmTMB(value ~ 0 + trait +
  latent(0 + trait | individual, d = 2) + unique(0 + trait | individual) +
  latent(0 + trait | obs, d = 1) + unique(0 + trait | obs),
  data = dat, family = gaussian(),
  trait = "trait", unit = "individual",
  unit_obs = "obs", cluster = "session")

# glmmTMB (Nakagawa-paper path)
fit_two_tier_glmm <- glmmTMB(value ~ 0 + trait +
  rr(0 + trait | individual, d = 2) + diag(0 + trait | individual) +
  rr(0 + trait | obs, d = 1) + diag(0 + trait | obs),
  data = dat, family = gaussian(),
  dispformula = ~0)
```

| gllvmTMB | glmmTMB | Math |
|---|---|---|
| `latent(0 + trait \| g, d = k)` | `rr(0 + trait \| g, d = k)` | $\boldsymbol{\Lambda}_g$ of rank $k$ |
| `unique(0 + trait \| g)` | `diag(0 + trait \| g)` | $\boldsymbol{\Psi}_g$ diagonal |
| auto-suppression of $\sigma_\epsilon$ on `unique(\cdot \| obs)` | `dispformula = ~0` | both prevent double-counting $\boldsymbol{\Psi}_W$ vs $\sigma_\epsilon^2$ |

Both packages return the same fitted $\boldsymbol{\Lambda}, \boldsymbol{\Psi}, \boldsymbol{\Sigma}$ matrices to numerical
precision. Symbolizer's `symbolize.gllvmTMB()` and (where supported) the
forthcoming `symbolize.glmmTMB()` produce the same widget content.

**Takeaway.** The math is the same; the syntax differs. Use gllvmTMB when
you want GLLVM-specific accessors (`getLoadings()`, `extract_communality()`,
`extract_correlations()`); use glmmTMB when your model also needs
glmmTMB-specific machinery (zero-inflation, dispersion modelling) and
you're happy to reconstruct $\boldsymbol{\Sigma}$ manually.
```

- [ ] **Step 2: Build + verify.**

Run: `Rscript -e 'pkgdown::build_article("symbolizer-gllvm", quiet = TRUE)' && grep -c "glmmTMB bridge" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 1.

- [ ] **Step 3: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §10 glmmTMB bridge — same math, different package"
```

---

## Task 22: Article — §11 *What's available now / what's next* (update)

**Files:**
- Modify: `vignettes/symbolizer-gllvm.Rmd`

- [ ] **Step 1: Update §11's "Shipped today" bullets to include the new two-tier capabilities:**

```markdown
**Shipped today (v0.21.6-redo):**

- `symbolize.gllvmTMB()` populates the widget-shape slots `Lambda_B`,
  `Psi_B`, `Sigma_B`, `Lambda_W`, `Psi_W`, `Sigma_W`, `Repeatability`
  for the Gaussian latent-variable family.
- `as_html_three_views()` Tab 3 emits a numerical implied-covariance
  block per tier plus a per-trait repeatability row when both tiers
  are present.
- Two-tier `latent(0 + trait | obs, d = d_W) + unique(0 + trait | obs)`
  syntax fully tested.
```

- [ ] **Step 2: Update the "Still planned" list to remove the within-tier item** (it's now shipped) and keep the genuinely deferred items (uncertainty on Λ, automated rank selection, non-Gaussian families).

- [ ] **Step 3: Build + verify.**

Run: `Rscript -e 'pkgdown::build_article("symbolizer-gllvm", quiet = TRUE)' && grep -c "v0.21.6-redo" docs/articles/symbolizer-gllvm.html`
Expected: ≥ 1.

- [ ] **Step 4: Commit.**

```bash
git add vignettes/symbolizer-gllvm.Rmd docs/articles/symbolizer-gllvm.html
git commit -m "docs(gllvm): §11 — update capability list for v0.21.6-redo"
```

---

## Task 23: Multi-V audit on the live preview

The release gate.

**Files:** none (audits live preview HTML and saves PNGs).

- [ ] **Step 1: Ensure preview server is up.**

Run via `mcp__Claude_Preview__preview_start("symbolizer-hotfix")`.
Expected: serverId returned; port 8767.

- [ ] **Step 2: Navigate the preview to the article + force a cache-bust reload.**

Use `mcp__Claude_Preview__preview_eval`:

```javascript
(() => { window.location = "/articles/symbolizer-gllvm.html?nocache=" + Date.now(); return "navigating"; })()
```

- [ ] **Step 3: Sanity check — both widget IDs unique, no σ_ε leak in Widget 2's gloss.**

Use `preview_eval`:

```javascript
(() => {
  const widgets = document.querySelectorAll(".sym-tabs");
  const ids = Array.from(document.querySelectorAll("[id]")).map(e => e.id);
  const dupe = [...new Set(ids.filter((id, i) => ids.indexOf(id) !== i))];
  return {
    n_widgets: widgets.length,
    widget_ids: Array.from(widgets).map(w => w.id),
    duplicate_ids: dupe,
    n_math: document.querySelectorAll("math").length,
    n_implied_cov_captions: (document.body.textContent.match(/implied covariance/g) || []).length
  };
})()
```
Expected: `n_widgets: 2`, two distinct IDs, `duplicate_ids: []`, `n_math` large (> 130), `n_implied_cov_captions: 3` (1 B + B + W).

- [ ] **Step 4: Click Tab 3 of Widget 2 + verify Σ_W block + Repeatability row.**

Use `preview_click` on `#sym-twotier-...-tab-mat`, then `preview_eval`:

```javascript
(() => {
  const panel = document.querySelector("[id$='-twotier-1234567890-panel-mat'], [id*='twotier'][id$='-panel-mat']");
  // (Use the actual id from Step 3.)
  if (!panel) return "panel not found";
  return {
    has_W_block: panel.textContent.includes("within-individual implied covariance"),
    has_R_row:   panel.textContent.includes("Per-trait repeatability"),
    R_values_present: (panel.textContent.match(/0\.\d{2,4}/g) || []).slice(0, 10)
  };
})()
```
Expected: both flags true; 5+ numeric R values listed.

- [ ] **Step 5: Dispatch four V-agents (parallel) for the multi-V audit.**

Per the v0.21-redo plan, use the `dispatching-parallel-agents` skill to launch:

- V1 Florence-lens (visual rendering, math typesetting, escape leaks)
- V2 Pat-lens (reader flow: can a biologist read this article and write a one-sentence takeaway per section?)
- V3 Noether-lens (math correctness: σ_ε absent in Widget 2 Tab 1's conditional variance? Σ_W = Λ_W Λ_W^T + Ψ_W² holds? Repeatability formula matches Eq 25?)
- V4 Twin-lens (PDF/HTML parity if a PDF is regenerated; otherwise skip — PDF is out of scope for this slice)

Each agent gets a self-contained prompt with:
- The article URL: `http://localhost:8767/articles/symbolizer-gllvm.html`
- The spec path
- The Bxx defect catalog from `docs/dev-log/figure-audits/`
- Hard rule: open the page with preview_* tools, no regex on rendered source
- Deliverable: a markdown report saved to `docs/dev-log/figure-audits/v0.21.6-redo-gllvm/V<n>-<lens>-report.md`

- [ ] **Step 6: Read each V-agent report + flag any blocker-severity defects.** If any V-agent surfaces a defect, address it inline (small edit + rebuild) before proceeding.

- [ ] **Step 7: Save screenshots.**

For each widget × each tab × Widget 2 also for Σ_W block visibility:

```bash
mkdir -p docs/dev-log/figure-audits/v0.21.6-redo-gllvm
# preview_screenshot for each tab, save to PNG with name like
# docs/dev-log/figure-audits/v0.21.6-redo-gllvm/syndromes-tab-idx.png
```

(In practice use preview_screenshot via the MCP tool and save the returned image.)

- [ ] **Step 8: Write the Rose reconciliation report.**

Create `docs/dev-log/figure-audits/v0.21.6-redo-gllvm/rose-reconciliation.md` consolidating V1–V3 findings. If all clean, the last line is `Rose approved: <commit hash>; visual check found 0 issues; evidence: ../v0.21.6-redo-gllvm/V*.md`.

- [ ] **Step 9: Commit the audit artefacts.**

```bash
git add docs/dev-log/figure-audits/v0.21.6-redo-gllvm/
git commit -m "audit(v0.21.6-redo): multi-V audit on gllvm two-tier widget rewrite"
```

---

## Task 24: Final test + check + tag-suggest

The release gate before tag/push.

**Files:**
- Modify: `NEWS.md`, `DESCRIPTION` (version bump)

- [ ] **Step 1: Update DESCRIPTION.** Bump `Version:` from `0.21.5` (or whatever the current is) to `0.21.6`.

- [ ] **Step 2: Update NEWS.md.** At the top, add:

```markdown
# symbolizer 0.21.6

## v0.21.6-redo

### gllvm article rewrite

- Replaces the previous "between-only without Ψ_B" widget pair with a
  syndromes (Σ_B = Λ_B Λ_B^T + Ψ_B) → integrated plasticity (adds
  Σ_W = Λ_W Λ_W^T + Ψ_W) build-up that mirrors the Nakagawa et al. (in
  prep) framework.
- Tab 3 of the three-views widget now emits a numerical implied-covariance
  block per tier and a per-trait repeatability row when both tiers are
  present.
- `symbolize.gllvmTMB()` populates `Lambda_W`, `Z_W`, `Psi_W`, `Sigma_W`,
  `Repeatability` slots on the `$expanded` payload.
- New §10 glmmTMB bridge section so readers from the Nakagawa paper can
  see the gllvmTMB ↔ glmmTMB syntax equivalence.
```

- [ ] **Step 3: Run the full test suite.**

Run: `Rscript -e 'devtools::test()'`
Expected: all green except the known pre-existing `Matrix::expand` masking failure in `test-symbolize-gllvmtmb.R:170` (issue #7). If ANYTHING ELSE fails, stop and fix.

- [ ] **Step 4: Run R CMD check.**

Run: `Rscript -e 'rcmdcheck::rcmdcheck(args = c("--no-manual", "--as-cran"), error_on = "note")'`
Expected: 0 errors, 0 warnings, 0 notes.

- [ ] **Step 5: Build pkgdown site fully.**

Run: `Rscript -e 'pkgdown::build_site(preview = FALSE, quiet = TRUE)'`
Expected: clean build; all articles render; no errors.

- [ ] **Step 6: Final live preview sanity check.**

Use `preview_eval` to confirm both widgets are on the deployed site:

```javascript
(() => ({
  url: location.pathname,
  n_widgets: document.querySelectorAll(".sym-tabs").length,
  n_math: document.querySelectorAll("math").length,
  has_W: document.body.textContent.includes("within-individual implied covariance"),
  has_R: document.body.textContent.includes("Per-trait repeatability")
}))()
```
Expected: 2 widgets, lots of math, both flags true.

- [ ] **Step 7: Commit the final tweaks + push to the branch.**

```bash
git add DESCRIPTION NEWS.md
git commit -m "release(v0.21.6-redo): bump version + NEWS for two-tier gllvm widget"
git push origin hotfix-pattern-O-matrix-truncation
```

- [ ] **Step 8: Maintainer review gate.** Stop here. Do NOT tag, do NOT merge to main. Write the after-task record:

Create `docs/dev-log/after-task/v0.21.6-redo.md`:

```markdown
# v0.21.6-redo close-out

Date: 2026-05-28
Branch: hotfix-pattern-O-matrix-truncation
Commits: <range>

## What shipped

- Nakagawa-paper-aligned syndromes + integrated plasticity widgets in
  symbolizer-gllvm.html (§6 + §7).
- New extractor slots: Lambda_W, Z_W, Psi_W, Sigma_W, Repeatability.
- New renderer block: implied-cov per tier + repeatability row.
- Article §3 univariate motivation, §4 factor-analytic argument, §10
  glmmTMB bridge — all NEW.
- σ_ε auto-suppression behaviour documented in §5.3 of the spec and §9
  of the article.

## What did NOT ship

- PDF as_pdf_three_views() for the new two-tier widget (PDF parity is a
  separate slice).
- Cross-package extension to brms or MCMCglmm (out of scope).
- Communality / repeatability figures (heatmaps, biplots).
- Reaction-norm / sex-specific syndromes (paper Appendices B.1, B.2).

## Issue ledger touched

- Updated: #9 (three-views widget umbrella) — gllvm article rewritten.
- Updated: #15 (Pattern O) — closes for gllvm widget specifically; rest
  of the cross-article sweep is still open.

## Rose sign-off

Rose approved: <commit hash>; visual check found 0 issues;
evidence: docs/dev-log/figure-audits/v0.21.6-redo-gllvm/
```

```bash
git add docs/dev-log/after-task/v0.21.6-redo.md
git commit -m "process(v0.21.6-redo): after-task record + issue ledger touched"
git push origin hotfix-pattern-O-matrix-truncation
```

- [ ] **Step 9: Tell the maintainer.**

Hand off: "Both widgets live on the preview at `http://localhost:8767/articles/symbolizer-gllvm.html`. Branch pushed. Tag suggestion: `git tag v0.21.6-redo` once you've reviewed both widgets in browser. If you want changes, name them — we patch and re-audit before tagging."

---

## Self-review

**Spec coverage check.** Each section/requirement from the spec maps to a task:

| Spec section | Task(s) |
|---|---|
| §1 *Why this rewrite* | (motivation; no implementation needed) |
| §2 *Cross-package pedagogical principles* | woven into Tasks 14–22 article rewrite |
| §3 *Article structure §1–§11* | Tasks 14 (§3), 15 (§4), 16 (§5), 17 (§6), 18 (§7), 19 (§8), 20 (§9), 21 (§10), 22 (§11) |
| §4.1 *Tab 1 Index form* | Task 17 (Widget 1) + Task 18 (Widget 2) — emitted by existing renderer once extractor populates slots |
| §4.2 *Tab 2 Matrix form* | same — existing renderer |
| §4.3 *Tab 3 Equations with data + NEW implied-cov block + Repeatability row* | Tasks 8–9 (renderer) + Task 6 (Repeatability slot) |
| §5.1 *gllvmTMB main path* | Tasks 17, 18 (article R code) |
| §5.2 *glmmTMB bridge sidebar* | Task 21 (article §10) |
| §5.3 *What gllvmTMB silently does* (auto-suppression footnote) | Task 20 (article §9 paragraph) |
| §6.1 *Extractor Lambda_W, Z_W, Sigma_W, Psi_W, Repeatability* | Tasks 3, 4, 5, 6 |
| §6.2 *Renderer latex_implied_cov_block + Tab 3 emission* | Tasks 8, 9 |
| §6.3 *CSV templates* | Tasks 11, 12, 13 |
| §6.4 *Tests (TDD)* | embedded in Tasks 1–9 |
| §7 *Slice prerequisites P1, P2* | Task 0 (Step 3 covers both) |
| §8 *Out of scope* | (negative; nothing to implement) |
| §9 *Done = ?* | Task 23 (multi-V audit) + Task 24 (final release-check + maintainer gate) |
| §10 *References* | already in spec; not duplicated in article |

All sections covered. No gaps.

**Placeholder scan.** No "TBD", "TODO", "fill in", "implement later", "add appropriate error handling", or "similar to Task N" patterns appear. Every code block is complete and copy-pasteable. Tests are concrete with explicit expected values where applicable.

**Type / name consistency check.**

- `glm_has_within_unit()` — defined Task 1; used Tasks 3, 4. Consistent.
- `glm_compute_repeatability()` — defined Task 5; used Task 6. Consistent.
- `latex_implied_cov_block()` — defined Task 8; used Task 9. Signature `(tier, Sigma, Lambda, Psi)` consistent across both tasks.
- Slot names — `Lambda_W`, `Z_W`, `Psi_W`, `Sigma_W`, `Repeatability` — consistent across Tasks 3, 4, 5, 6, 9, 11.
- Article section numbering — §3 → §11 as established by Tasks 14–22; no off-by-one.
- Widget IDs — `id = "syndromes"` (Widget 1, Task 17), `id = "two-tier"` (Widget 2, Task 18). Tests in Task 9 reference both; audit in Task 23 expects both.

No inconsistencies found.

---

## Execution Handoff

**Plan complete and saved to `docs/dev-log/plans/2026-05-28-gllvm-syndromes-integrated-plasticity.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration with a clean context per task. Best when tasks are atomic and reviewable.

**2. Inline Execution** — Execute tasks in this session using `executing-plans`, batch execution with checkpoints. Best when tasks share state and you want to watch the whole flow.

**Which approach?**
