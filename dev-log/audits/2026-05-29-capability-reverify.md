# Package-capability re-verification — 2026-05-29

Workflow: 8 agents, each fit a real model of its package, ran symbolize()/as_latex()/interpretation, and checked against inst/extdata/capabilities.csv. symbolizer 0.22.3.

Problem packages (errors / wrong-output / overclaim): **mgcv, brms, metafor, phylolm, sdmTMB**

| package | symbolize | registry | top issues |
|---|---|---|---|
| lme4 | works | partial | MAJOR: methods_text() prose renderer has no template for glmerMod families (errors on supported binomial/poisson) |
| glmmTMB | works | partial | MAJOR: assumption_table() / assumptions slot corrupted for glmmTMB poisson, nbinom2, binomial (all registry First slice) |
| mgcv | wrong_output | overclaims | BLOCKER: Non-Gaussian GAM with ANY smooth term is rejected, despite registry claiming gam,Gamma/poisson/binomial,mu as First slice; MAJOR: Inverse-link Gamma (mgcv's DEFAULT) yields mathematically wrong, self-contradictory prose: assumptions/interpretation/three-views all say 'log link'; MAJOR: mgcv Gamma scale/dispersion is dropped numerically and mis-modeled as a per-observation dispersion submodel |
| brms | works | overclaims | MAJOR: Phylo equation omits the A correlation matrix: rendered identically to an iid (1|g) random intercept; MAJOR: Meta-analytic se(sqrt(vi)) equation uses a free residual sigma_i instead of known v_i |
| MCMCglmm | works | partial | MAJOR: Pedigree animal model renders TREE-only assumption prose that is biologically wrong and self-contradictory |
| metafor | wrong_output | overclaims | BLOCKER: Categorical moderator in rma.uni meta-regression crashes symbolize(); MAJOR: rma.mv factor moderator silently mis-rendered as a continuous predictor (loses contrast structure); MAJOR: No-intercept formula (~ x - 1 / ~ outcome - 1) injects a phantom beta_0 into fixed_effects, LaTeX, and interpretation prose |
| phylolm | wrong_output | overclaims | MAJOR: Matrix-form distribution asserts a DIAGONAL residual covariance, which is wrong for PGLS; MAJOR: as_latex() model equation omits the phylogenetic correlation entirely (both notations); MAJOR: Assumptions table self-contradicts: an 'independence' row fires alongside the phylo covariance |
| sdmTMB | works | overclaims | MAJOR: Spatial/spatiotemporal random fields rendered as plain IID random intercepts, not the GMRF the registry promises; MAJOR: Delta / two-component models crash with opaque base-R error instead of the intended 'Planned or reserved' routing |

---

## lme4  (symbolize=works, registry=partial)
Classes checked: lmerMod, glmerMod
- **[MAJOR] methods_text() prose renderer has no template for glmerMod families (errors on supported binomial/poisson)**
  - methods_text(symbolize(glmer(...,family=poisson()))) and family=binomial() both abort: 'No methods_text() template for class "glmerMod" / family "poisson" [/"binomial"]'. The shipped template list includes lmerMod/gaussian but NO glmerMod entry. lmerMod/gaussian methods_text() works fine. Registry lists glmerMod binomial/poisson as 'First slice' supported with no component restriction, so a user on a supported glmer family who calls the documented methods-prose renderer hits a hard error. as_latex() and the interpretation table both work for these fits, so it is a renderer-coverage gap, not a total failure.
- **[MINOR] cbind(successes, failures) binomial labels the response as only the first column**
  - glmer(cbind(succ,fail) ~ x + (1|g), family=binomial()) symbolizes but response resolves via all.vars(form[[2L]])[[1L]] to 'succ'; LaTeX reads 'succ | mu ~ Binomial(n_i, mu)'. The model shape (Binomial with n_i trials) is mathematically fine, but the response label drops the successes/trials structure (cbind(succ,fail)).
- **[NIT] Bernoulli (0/1) response rendered as Binomial(n_i, mu) with unexplained n_i**
  - glmer(y~x+(1|g)) with y in {0,1} (weights all 1) renders 'y | mu ~ Binomial(n_i, mu)' rather than 'Bernoulli(mu)'. This is a common convention and not incorrect, but n_i is undefined/equal to 1 for a 0/1 response and is not explained in the symbol dictionary.
- **[OK] lmerMod Gaussian symbolizes correctly (distribution, link, fixed effects, CIs, variance components)**
  - lme4::lmer(body_mass ~ temperature + (1|g)) -> symbolize() gives model$class=lmerMod, family=gaussian. distribution = Normal(mu, sigma^2); submodel link=identity. fixed_effects estimates match fixef() exactly: (Intercept)=30.7848, temperature=0.379072. variance_components correctly split RE g (Intercept) sd=1.0390 from residual sd=sigma(fit)=1.320992. as_latex shows: y|mu,sigma ~ Normal(mu_i, sigma_i^2); mu_i = beta0 + beta1*temperature_i + u_g(i); u_g ~ N(0, sigma_g^2). Both Wald (default) and profile (ci_method='profile') CI paths work.
- **[OK] glmerMod binomial and poisson symbolize with correct distribution/link**
  - glmer(y~x+(1|g),family=binomial()) -> family=binomial, link=logit, LaTeX 'y|mu ~ Binomial(n_i, mu); logit(mu_i)=...; u_g~N(0,sigma_g^2)'. glmer(...,family=poisson()) -> family=poisson, link=log, LaTeX 'y|mu ~ Poisson(mu); log(mu_i)=...'. interpretation table natural_scale_reading is correct: poisson 'Expected count multiplied by exp(beta)'; binomial 'odds ratio ...'/plogis baseline. Matches registry glmerMod binomial/poisson mu+random_effects 'First slice'.
- **[OK] Random slope (1 + x | g) renders the correct 2x2 covariance matrix**
  - lmer(y ~ temperature + (1+temperature|g)) -> as_latex emits mu_i = b0 + b1*temp_i + u_{0,g(i)} + u_{1,g(i)}*temp_i, then (u0,u1)^T ~ N_2(0, Sigma_u,g) with the full 2x2 Sigma block (sigma_u0^2, rho*sigma_u0*sigma_u1; ...; sigma_u1^2). variance_components lists g (Intercept), g temperature, and residual. Matches the registry note 'Random intercepts (1|g) and (1+x|g) on the conditional submodel'.
- **[OK] Capability gating blocks unsupported glmer families (no over-claim)**
  - glmer(y~x+(1|g), family=Gamma(link='log')) -> symbolize() aborts with 'symbolize() cannot read glmerMod / Gamma / mu yet (status: Planned or reserved)' via capability_check('glmerMod', family, 'mu'). The glmerMod *,* row is 'Planned or reserved', so this is correct gating.

**Verdict:** lme4 support is real and mathematically correct for the registry-claimed surfaces. All three claimed combinations symbolize live without error on lme4 1.1.37 / symbolizer 0.22.3: lmerMod gaussian (identity, Normal(mu,sigma^2), fixef matches exactly, RE SD vs residual sigma correctly separated, Wald+profile CIs), glmerMod binomial (logit, Binomial(n_i,mu)), and glmerMod poisson (log, Poisson(mu)); random intercept (1|g) and random slope (1+x|g) both render the correct N(0,sigma^2) / 2x2 Sigma covariance blocks, and the interpretation table gives correct exp()/odds-ratio readings. Capability gating correctly BLOCKS unsupported families (Gamma glmer aborts cleanly). registry_match is 'partial' for one reason: the methods_text() prose renderer ships a template for lmerMod/gaussian but NONE for glmerMod, so methods_text() hard-errors ('No methods_text() template for class glmerMod / family poisson|binomial') on glmer families the registry advertises as 'First slice'. as_latex() and the interpretation table work for those fits, so it is a renderer-coverage gap rather than a core symbolize() over-claim, but it is a documented surface that fails -- hence MAJOR. Minor/nit: cbind(succ,fail) binomial labels the response as only the first column ('succ'), and 0/1 Bernoulli responses render as Binomial(n_i,mu) with an unexplained n_i. Exact fit code used (representative): lme4::lmer(body_mass ~ temperature + (1|g), data) for Gaussian; lme4::glmer(y ~ x + (1|g), data, family=binomial()) and family=poisson() for glmer; lme4::glmer(y ~ temperature + (1+temperature|g), data) for the random slope; lme4::glmer(cbind(succ,fail) ~ x + (1|g), family=binomial()) and family=Gamma(link='log') for the edge cases. Source: /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/R/symbolize-lme4.R; registry rows at lines 92-99 of /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/inst/extdata/capabilities.csv.

## glmmTMB  (symbolize=works, registry=partial)
Classes checked: glmmTMB
- **[MAJOR] assumption_table() / assumptions slot corrupted for glmmTMB poisson, nbinom2, binomial (all registry First slice)**
  - Root cause: shared inst/extdata/assumption-templates.csv has 12 rows whose expression_latex field is UNQUOTED yet contains commas inside LaTeX (e.g. line 68: `poisson,,non_negative_integer_response,{response_symbol} \in \{0\, 1\, 2\, \ldots\},"...",implied,`). utils::read.csv() in load_template() (R/templates.R) splits each `\, ` as a field separator, shifting columns right. Result for poisson non_negative_integer_response in the LIVE assumptions tibble: expression_latex='{response_symbol} \in \{0\' (invalid/truncated LaTeX), biological_meaning='1\', status='2\'. Same for nbinom2 non_negative_integer_response, the poisson/nbinom2 zi+hu linear_predictor rows, and binomial response_in_zero_to_n (status='\ldots\'). gaussian is UNAFFECTED (its conditional_distribution row is quoted in the CSV). symbolize() does not error and as_latex() (the headline equation output) is fine; only the assumption text is broken. This is a data defect in the shared CSV (affects every class routing these families through drm_build_assumptions: drmTMB, glm, glmmTMB), not a glmmTMB-extractor bug. Existing tests pass because none assert these specific LaTeX strings. Spawned a spinoff task to fix the CSV quoting.
- **[NIT] methods_text() ships only for glmmTMB/gaussian, errors for other glmmTMB families**
  - methods_text(symbolize(nbinom2_fit)) aborts: 'No methods_text() template for class glmmTMB / family nbinom2' with a clean cli list of available templates (glmmTMB/gaussian only). methods_text is a separate renderer not gated by capabilities.csv, so this is a renderer template gap rather than a symbolize over-claim. methods_text(glmmTMB/gaussian) works and reads correctly ('We fit a Gaussian linear mixed model ... random intercept for g ... 95% Wald CIs').
- **[OK] symbolize() + as_latex() correct for all registry-claimed glmmTMB families**
  - Live fits ran cleanly and produced mathematically correct equations: gaussian -> Normal(mu_i, sigma_i^2), identity link, RE rendered as u_{g(i)} ~ N(0, sigma_g^2); poisson -> Poisson(mu_i), log link, no dispersion; nbinom2 -> NegBin(mu_i, size=exp(sigma_i)) with Var=mu+mu^2/exp(sigma), log link; binomial -> Binomial(n_i, mu_i), logit link; zi-poisson -> adds logit(pi_{zi,i}) submodel; dispformula -> adds log(sigma_i)=gamma_0+gamma_1*temperature. Estimates/CIs/variance components recovered correctly (e.g. gaussian intercept~30, slope~0.4, group SD~1.6, residual~1.5). All 3 existing glmmTMB test files pass (test-symbolize-glmmtmb.R, -families.R, -zi.R).
- **[OK] propto() structured-covariance (phylo) pattern handled correctly and honestly**
  - Fit y ~ 1 + (1|study) + propto(0+obs|g, V) sets metadata$detected_signals='phylo', phylo_representation='tips_only', and emits a warning_table() info row 'propto_known_corr_block' that correctly states Sigma = sigma^2 * V (sigma^2 ESTIMATED, phylogenetic/pedigree pattern) and explicitly distinguishes it from the meta-analytic fixed-V pattern (metafor::rma.mv(V=V,...), Sigma=V exactly). This matches the registry rows glmmTMB/{gaussian,binomial,poisson}/propto and glmmTMB/*/phylo (First slice). Non-misleading.
- **[OK] Beta family correctly blocked by capability gate (matches registry)**
  - glmmTMB beta_family() fit -> symbolize() aborts with: 'symbolize() cannot read glmmTMB / beta / mu yet (status: Planned or reserved)' plus a helpful 'today we can read' list. Registry has no glmmTMB,beta row, so only the wildcard glmmTMB,*,* (Planned or reserved) matches. Correct gate, not an over-claim.

**Verdict:** glmmTMB support in symbolizer is solid at the core: symbolize() runs without error and as_latex() is mathematically correct for every registry-claimed family I exercised live (gaussian +RE +dispformula-sigma, poisson, nbinom2 +RE, binomial, zi-poisson) and for the glmmTMB-specific propto()/phylo structured-covariance pattern, which is handled with unusually careful, non-misleading wording (Sigma = sigma^2 * V with sigma^2 estimated, explicitly NOT the fixed-V meta-analytic case). The beta family correctly errors via the capability gate, matching the registry (no glmmTMB/beta row). All 12 existing glmmTMB tests pass. registry_match is 'partial' because of one MAJOR defect that degrades a claimed surface without being an outright symbolize over-claim: the assumption_table() renderer and the underlying assumptions slot emit corrupted LaTeX/text for poisson, nbinom2, and binomial (all First slice). The cause is a shared-CSV bug -- inst/extdata/assumption-templates.csv has 12 rows whose expression_latex field is unquoted but contains commas inside LaTeX (e.g. `\\{0\\, 1\\, 2\\, \\ldots\\}`), so read.csv() in load_template() splits the field and shifts columns, yielding e.g. poisson non_negative_integer_response = expression `{response_symbol} \\in \\{0\\`, meaning `1\\`, status `2\\`. gaussian is unaffected (its row is quoted). This affects every model class that routes these families through drm_build_assumptions, not just glmmTMB; I spawned a spinoff task to fix the CSV quoting and add a regression test. Secondary NIT: methods_text() only ships a glmmTMB/gaussian template and errors (cleanly) for other glmmTMB families. Representative fit code used: `library(glmmTMB); set.seed(13); dnb<-data.frame(y=rnbinom(120,mu=3,size=2),x=rnorm(120),grp=factor(rep(1:8,15))); fit<-glmmTMB(y~x+(1|grp),data=dnb,family=nbinom2()); sym<-symbolizer::symbolize(fit); symbolizer::as_latex(sym); symbolizer::assumption_table(sym)`. The TMB version-mismatch warning (built 1.9.17 vs current 1.9.21) appeared but did not affect fits. Relevant files: /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/R/symbolize-glmmtmb.R, /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/R/symbolize-drmtmb.R (drm_build_assumptions ~L1901), /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/R/templates.R (load_template), /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/inst/extdata/assumption-templates.csv (lines 59,67,75,84,92,100,109,111,113,115,117,130), /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix/inst/extdata/capabilities.csv.

## mgcv  (symbolize=wrong_output, registry=overclaims)
Classes checked: gam (mgcv::gam, gaussian/identity), gam (mgcv::gam, Gamma/inverse = mgcv default), gam (mgcv::gam, Gamma/log), gam (mgcv::gam, poisson/log), gam (mgcv::gam, binomial/logit), bam (alias: symbolize.bam <- symbolize.gam)
- **[BLOCKER] Non-Gaussian GAM with ANY smooth term is rejected, despite registry claiming gam,Gamma/poisson/binomial,mu as First slice**
  - symbolize.gam runs capability_check("gam", family, "mu") then, when smooths exist, capability_check("gam", family, "smooth"). capabilities.csv has only one smooth row: line 115 `gam,gaussian,smooth,First slice`. There is NO `gam,Gamma,smooth` / `gam,poisson,smooth` / `gam,binomial,smooth` row, so non-Gaussian smooth GAMs fall through to line 119 `gam,*,*,Planned or reserved` and abort. Live evidence: `gam(ygam ~ s(x) + z, family=Gamma(), method="REML")` -> ERROR: "symbolize() cannot read gam / Gamma / smooth yet (status: Planned or reserved)". Identical errors for `family=poisson()` (gam / poisson / smooth) and `family=binomial()` (gam / binomial / smooth). The SAME models WITHOUT a smooth (e.g. `gam(ygam ~ x + z, family=Gamma())`) return OK. A GAM without smooths is just a GLM, so the headline claim `gam,Gamma,mu First slice` is effectively unusable for actual additive models. This is the Gamma defect the maintainer flagged, and it extends to poisson and binomial. Over-claim => BLOCKER.
- **[MAJOR] Inverse-link Gamma (mgcv's DEFAULT) yields mathematically wrong, self-contradictory prose: assumptions/interpretation/three-views all say 'log link'**
  - mgcv's Gamma() defaults to link='inverse' (1/mu = eta), unlike the drmTMB/glm 'Gamma == log link' convention baked into the package. For `gam(ygam ~ x + z, family=Gamma(), method="REML")` (link captured correctly as 'inverse' in sym$submodels$link and rendered `\mathrm{inverse}(\mu_i)=...` in distribution/as_latex), the prose layers are WRONG and contradict the equation: (a) sym$assumptions linear_predictor row expression = `\log(\mu_i) = ...`, biological_meaning = 'log link on mu'; (b) sym$interpretation$link_scale_reading = 'Log mean of ygam changes by -0.236 per unit of x'; natural_scale_reading = 'Mean of ygam multiplied by exp(-0.236) per unit of x'; (c) as_html_three_views coefficient-reading box: 'On the response scale, $\exp(\hat\beta)$ multiplies the mean ... (log link)' printed directly above an equation panel that reads `\mathrm{inverse}(\mu_i)=...`. Direction is also inverted: true DGP mean increases in x (mu=exp(1+0.7x)); inverse link flips the sign so beta_x=-0.236 and the tool tells the reader the mean is multiplied by exp(-0.236)<1 (decreasing). Root cause: drm_build_assumptions/interpretation read family-parameterizations.csv (Gamma link_mu='log') and render-three-views.R line ~573 hard-codes `gamma = "\\exp"` link_label, neither consults the actual fit link. The log-link Gamma (`family=Gamma(link="log")`) renders correctly. Misleading scientific output => MAJOR.
- **[MAJOR] mgcv Gamma scale/dispersion is dropped numerically and mis-modeled as a per-observation dispersion submodel**
  - mgcv estimates a single Gamma scale parameter phi (summary(fit)$dispersion = 0.262 for the test fit). symbolizer's Gamma distribution row displays a per-observation dispersion SUBMODEL: `Gamma(shape = 1/sigma_i^2, scale = mu_i*sigma_i^2)` with sigma_i in the symbol dictionary ('Gamma dispersion (on the log scale)'). But sym$variance_components is EMPTY (0 rows) for the Gamma fit -- mgcv_build_variance_components only captures fit$sig2 for gaussian and fit$sp for smooths, never the Gamma scale. So sigma_i appears in every rendered equation with no estimate behind it, and is presented as varying per i when mgcv fits one constant scale. as_latex therefore shows shape=1/sigma_i^2 with sigma_i undefined numerically. Incorrect parameterization/dimensionality for this model class => MAJOR.
- **[NIT] Literal 'NA ' prefix in raw components$equation for the mu row**
  - sym$components$equation for the mu submodel begins with a literal 'NA': e.g. Gamma inverse -> `NA\mathrm{inverse}(\mu_i) = beta_0 + ...`; Gaussian+smooth -> `NA \mu_i = beta_0 + beta_1 z_i + f_{1}(x_i)`. as_latex() and as_html_three_views() strip/avoid it so the user-facing LaTeX is clean, but the stored component string is polluted (likely a missing link-label lookup leaving an NA glued to the link operator). Cosmetic; affects raw object inspection only.
- **[OK] Gaussian additive model (the only registry combo with a test) is correct end to end**
  - `gam(yg ~ s(x) + z, method="REML")` -> symbolize OK: family gaussian, link identity, smooth_count 1; mu equation `\mu_i = beta_0 + beta_1 z_i + f_{1}(x_i)`; metadata$smooths populated (label s(x), bs_dim 10, edf 6.87); as_latex contains f_{1}; variance_components has 2 rows (residual SD + smoothing param). Matches capabilities.csv rows 114-115 (gam,gaussian,mu + gam,gaussian,smooth First slice). This is the lone covered combo (test-symbolize-mgcv.R only asserts the Gaussian case; helper-mgcv-fits.R fixtures are all Gaussian).

**Verdict:** mgcv is installed (1.9.3) and symbolizer 0.22.3 supports it for ONE combination only: a Gaussian additive model, which works correctly end to end (distribution, identity link, smooth table, as_latex, variance components). Everything the registry additionally claims is over-claimed. The capability table (capabilities.csv lines 114-120) lists gam,Gamma,mu / gam,poisson,mu / gam,binomial,mu as 'First slice', but there is no gam,{Gamma,poisson,binomial},smooth row -- only gam,gaussian,smooth (line 115) -- so ANY non-Gaussian GAM that contains a smooth term (i.e. essentially every real GAM) is rejected with 'status: Planned or reserved' (BLOCKER). The exact reproducer the maintainer flagged: `mgcv::gam(ygam ~ s(x) + z, family=Gamma(link=\"log\"), method=\"REML\")` -> ERROR \"symbolize() cannot read gam / Gamma / smooth yet\"; the default-link variant `family=Gamma()` fails identically, as do poisson()/binomial() with s(x). Only smooth-free fits (degenerate GAMs) of those families succeed, and even then the Gamma output is wrong twice over: (i) for mgcv's DEFAULT inverse link, the assumptions block, interpretation readings, and the three-views coefficient-reading box all assert a log link ('\\log(\\mu_i)', 'exp(beta) multiplies the mean (log link)') while the equation itself correctly shows \\mathrm{inverse}(\\mu_i) -- a self-contradiction that inverts the reported effect direction (MAJOR); (ii) mgcv's single Gamma scale phi is never captured (variance_components empty) yet the distribution displays a per-observation dispersion submodel sigma_i with no value (MAJOR). Exact fit code used (all via Rscript -e 'library(symbolizer); library(mgcv); ...'): set.seed(1); n<-150; x<-runif(n); z<-runif(n); mu_true<-exp(1+0.7*x); ygam<-rgamma(n,shape=4,rate=4/mu_true); d<-data.frame(ygam,x,z); symbolize(gam(ygam~s(x)+z, family=Gamma(), data=d, method=\"REML\")) [ERRORS]; symbolize(gam(ygam~x+z, family=Gamma(), data=d, method=\"REML\")) [OK but wrong prose, inverse link]; symbolize(gam(ygam~x+z, family=Gamma(link=\"log\"), data=d, method=\"REML\")) [OK, correct]; Gaussian baseline symbolize(gam(yg~s(x)+z, data=..., method=\"REML\")) [OK]. This matches the deferred item explicitly logged under 'Did NOT do this session: Capability re-verification across ... mgcv-Gamma ...' in .memory/reports/2026-05-29-families-audit-punch-list.md. Recommended registry action: either downgrade gam,Gamma/poisson/binomial,mu to 'Planned or reserved' until non-Gaussian smooths + per-fit link handling land, or add the missing gam,{family},smooth rows AND fix link-aware assumptions/interpretation/three-views plus Gamma scale capture before keeping the First-slice claim.

## brms  (symbolize=works, registry=overclaims)
Classes checked: brmsfit
- **[MAJOR] Phylo equation omits the A correlation matrix: rendered identically to an iid (1|g) random intercept**
  - Live fit y~1+(1|gr(species,cov=A)) with data2=list(A=C). symbolize() correctly sets metadata$detected_signals='phylo', phylo_representation='tips_only', adds the \mathbf{A} structured_correlation_phylo dictionary row, and fires phylo assumption rows. BUT as_latex renders the random effect as 'u_{species} ~ N(0, sigma_species^2)' -- A is absent from the covariance, so the equation is indistinguishable from a plain iid random intercept, defeating the purpose of phylogenetic modelling. Direct contrast: the metafor extractor on the equivalent fit renders 'u_{phylogeny} ~ N(0, sigma_phylogeny^2 A)' (A present). Root cause: symbolize-brms.R builds structured_matrices + the dictionary row but never passes a group->\mathbf{A} mapping into the component/equation builder, unlike symbolize-mcmcglmm.R (L177-180) and symbolize-metafor.R (L400) which inject \mathbf{A} into the RE distribution line. Registry row 123 'brms,*,phylo,First slice' is satisfied for detection/metadata but the headline mathematical product (the equation) is misleading.
- **[MAJOR] Meta-analytic se(sqrt(vi)) equation uses a free residual sigma_i instead of known v_i**
  - Live fit bf(yi|se(sqrt(vi))~1). symbolize() correctly detects metadata$context='meta_analysis' (symbolize-brms.R L242-243). BUT family stays 'gaussian', so as_latex renders 'yi_i | mu_i, sigma_i ~ Normal(mu_i, sigma_i^2)' -- a freely-estimated residual SD. For a fixed-se meta-analysis the correct model has KNOWN per-study sampling variance: 'Normal(theta_i, v_i), v_i known'. The package already has the right template: family 'meta_normal' in family-distributions.csv L14 renders 'Normal(theta_i, v_i), v_i known', and the drmTMB extractor switches to it on meta_V() detection (symbolize-drmtmb.R L153-154,532-539); metafor likewise renders 'Normal(theta_i, v_i), v_i known'. The brms extractor never maps se() detection to the meta_normal family, so registry row 131 'brms,gaussian,meta_known_vi,First slice' over-claims: a reader transcribing the brms equation would write the wrong (free-variance) model.
- **[NIT] Bernoulli LaTeX shows Binomial(n_i, mu_i) with an explicit n_i**
  - For a bernoulli() fit, as_latex prints 'Binomial(n_i, mu_i)'; a Bernoulli has n=1 by construction, so the n_i is cosmetically imprecise. This follows directly from the documented, intentional bernoulli->binomial aliasing (symbolize-brms.R L68-72) and is shared with the binomial template, so it is low-impact.
- **[OK] Gaussian conditional submodel (mu, identity) is correct**
  - Live rstan fit body_mass~temperature (n=60). symbolize() -> family gaussian, distribution `Normal(mu_i, sigma_i^2)`, submodel link=identity/coef=beta. fixed_effects: intercept 30.4 (true 30), slope 0.389 (true 0.4), both with Q2.5/Q97.5 credible bands and ci_method='credible', excludes_zero=TRUE. as_latex renders mu_i = beta_0 + beta_1 temperature_i. parameter_interpretation() returns 2 non-empty rows with correct link/natural/biological readings and credible band. Matches registry row 75 (First slice).
- **[OK] Gaussian (1|g) random intercepts + variance components correct**
  - Live fit body_mass~temperature+(1|g). variance_components carries group 'g' (sd>0) plus 'residual' (sd>0); random_effects has group_var 'g' and component '(Intercept)'. Matches registry row 76 (First slice).
- **[OK] Distributional sigma~x location-scale model correct**
  - Live fit bf(y~x, sigma~x). Two submodels: mu (link=identity, coef=beta) and sigma (link=log, coef=gamma). as_latex: 'mu_i = beta_0 + beta_1 x_i' and 'log(sigma_i) = gamma_0 + gamma_1 x_i'. All four coefficients carry credible bands. Mathematically correct log-link scale submodel. Matches registry row 77 (First slice).
- **[OK] Binomial/Bernoulli mu (logit) correct for both bernoulli() and trials()**
  - Live bernoulli() fit y~x: family aliased to binomial, distribution `Binomial(n_i, mu_i)`, logit link, slope 1.13 (true 1.2). Live binomial() fit y|trials(n)~x: same distribution, slope 0.99 (true 1.0), intercept 0.25 (true 0.2). Matches registry rows 78-79 (First slice). bernoulli->binomial aliasing in symbolize-brms.R L72 is documented and intentional.
- **[OK] Poisson mu (log link) correct**
  - Live fit y~x, family=poisson(). distribution `Poisson(mu_i)`, submodel link=log, as_latex 'log(mu_i)=beta_0+beta_1 x_i'. slope 0.89 (true 0.8), intercept 0.41 (true 0.5). Matches registry rows 80-81 (First slice).
- **[OK] Capability gate rejects unsupported families cleanly**
  - Live fit y~x, family=Gamma(link='log'). symbolize(fit) errors via capability_check (cli_abort listing supported tuples), rather than emitting wrong output. Consistent with wildcard registry row 82 'brmsfit,*,* Planned or reserved'. Correct under-claim-safe behavior.

**Verdict:** brms support is installed (brms 2.22.0) and broadly solid: I built and live-fit (rstan backend) seven minimal models and ran symbolize()/as_latex()/parameter_interpretation() on each. The five core registry claims are verified mathematically correct -- Gaussian mu (identity), Gaussian (1|g) random intercepts + variance components, distributional sigma~x (log-link scale submodel), Binomial/Bernoulli mu (logit, both bernoulli() and trials()), and Poisson mu (log) -- all with Bayesian credible bands (ci_method='credible'), correct distributions/links, and point estimates that recover the data-generating truth. Unsupported families (Gamma) are correctly rejected by the capability gate. However, the two most recently added claims over-claim at the equation level. (1) Phylo via gr(species, cov=A): symbolize() detects the signal and documents A in the dictionary/assumptions, but as_latex renders the random effect as 'u_{species} ~ N(0, sigma_species^2)' with A absent -- visually identical to an iid (1|g) intercept -- whereas the sibling metafor/mcmcglmm extractors inject 'sigma^2 A' into the covariance (I confirmed metafor renders 'u_{phylogeny} ~ N(0, sigma^2 A)' on the equivalent fit). (2) Meta-analytic se(sqrt(vi)): symbolize() sets context='meta_analysis' but keeps family='gaussian', so the equation shows a free residual sigma_i^2 instead of the correct 'Normal(theta_i, v_i), v_i known' that the meta_normal family (used by the drmTMB/metafor paths) already provides. Both are MAJOR equation-fidelity defects because the headline product -- the equation a reader would transcribe -- is mathematically misleading for the very feature the registry advertises as supported. Exact representative fit code: `library(symbolizer); library(brms); set.seed(11); k<-8; tree<-ape::rcoal(k); tree$tip.label<-paste0('sp',1:k); C<-ape::vcv(tree,corr=TRUE); u<-as.numeric(crossprod(chol(C),rnorm(k))); dat<-data.frame(species=factor(tree$tip.label,levels=tree$tip.label), y=u+rnorm(k,sd=0.5)); fit<-brm(y~1+(1|gr(species,cov=A)), data=dat, data2=list(A=C), family=gaussian(), chains=1, iter=300, warmup=150, refresh=0, backend='rstan'); cat(as_latex(symbolize(fit)))` -> RE line 'u_{species} & \\sim \\mathcal{N}(0,\\, \\sigma_{species}^2)' (no A). Recommendation: for phylo, pass a group->\\mathbf{A} mapping into the component builder as mcmcglmm/metafor do; for meta, map se() detection to the meta_normal family. Registry status word 'First slice' is defensible for scope, but rows 123 and 131 should either be downgraded to reflect detection-only support or the extractors fixed to emit the structured/known-variance equations.

## MCMCglmm  (symbolize=works, registry=partial)
Classes checked: MCMCglmm
- **[MAJOR] Pedigree animal model renders TREE-only assumption prose that is biologically wrong and self-contradictory**
  - For the ginverse pedigree animal model (Ainv from inverseA(ped)), the assumptions tibble fires gaussian/phylo rows from inst/extdata/assumption-templates.csv lines 149-153 with tree-specific text: row 'phylo_random_effect' = 'Species-level random effect...'; 'phylo_A_positive_definite' = 'A...derived from a rooted tree under Brownian motion'; 'phylo_tips_only_representation' = 'Tips-only k x k representation: A_{ij} is shared branch length between species i and j divided by total tree height'; 'phylo_brownian_motion'; 'phylo_ultrametric_tree'. None apply to a pedigree (no tree, no branch lengths, no species; A is the additive-genetic relationship matrix). Worse, row 'phylo_tips_only_representation' DIRECTLY CONTRADICTS the extractor's own metadata$phylo_representation='all_nodes' and the symbol_dictionary description ('Hadfield-Nakagawa all-nodes sparse-precision representation'). The registry explicitly claims MCMCglmm,gaussian,animal (First slice) and the hint targets the pedigree path, so this misleading text ships on a claimed-supported capability. Test test-symbolize-mcmcglmm-animal.R:60-71 asserts these row KEYS are present (locking the behavior in) but does not assert the biological_meaning text, leaving the contradiction unguarded. Root cause: shared 'phylo' signal maps tree-based and pedigree-based A to identical tree-only templates; the all_nodes-vs-tips_only contradiction also affects genuine phylogenetic MCMCglmm use since ginverse is all-nodes by construction.
- **[NIT] Continuous predictors rendered without \mathrm wrap in LaTeX**
  - as_latex shows 'beta_1 \, temperature_i' (raw column name, no \mathrm{}). The response symbol IS wrapped (\mathrm{body\_mass}_i) per the v0.21.1 fix in mcmcglmm_resolve_response_symbol, but predictor names are not. For single-token names like 'temperature' this is MathJax-safe; a multi-underscore predictor name (e.g. log_mass) would mis-render as a subscript. Cosmetic / latent, not triggered by the tested fits.
- **[OK] Simple Gaussian (y ~ x) symbolizes correctly**
  - Fit: MCMCglmm(body_mass ~ temperature, data=dat, family='gaussian', nitt=1500,burnin=500,thin=2). symbolize(fit, data=dat) runs clean. parameterization = Normal / identity link / sigma = residual SD. fixed_effects match summary(fit)$solutions exactly: (Intercept) post.mean 30.092 [28.83, 31.48], temperature 0.405 [0.334, 0.474]; ci_method='credible'. variance_components: residual var 1.811 (= post.mean of Rcovariances). as_latex: y_i | mu_i,sigma_i ~ Normal(mu_i, sigma_i^2); mu_i = beta_0 + beta_1 temperature_i. Mathematically correct.
- **[OK] Gaussian + random intercept (random = ~g) correct**
  - Fit adds random=~g (10 groups). random_effects row: (1|g), n_levels=10, u_{g(i)}. variance_components: g random SD 2.187 (=sqrt(4.782 Gcovariance post.mean)), residual SD 1.729 (=sqrt(2.991)). as_latex appends '+ u_{g(i)}' to mu and a line 'u_g ~ N(0, sigma_g^2)'. Few-RE-levels warning correctly does NOT fire (10 >= 5 threshold). All correct.
- **[OK] Animal/phylo model via ginverse: structure, A-matrix, and heritability are correct**
  - Fit: MCMCglmm(phenotype ~ 1, random=~animal, ginverse=list(animal=inverseA(ped)$Ainv)). metadata: animal_groups='animal', phylo_representation='all_nodes', detected_signals='phylo'. variance_components animal row tagged kind='animal'. as_latex (index and matrix form) renders 'u_animal ~ N(0, sigma^2_animal A)' with structured matrix A (NOT I) -- correct Hadfield-Nakagawa animal-model form. Heritability derived correctly: h^2 = 2.003/(2.003+0.722) = 0.735, attached to metadata$heritability. n_levels overridden to nrow(Ainv)=30. symbol_dictionary documents A as 'structured_correlation_phylo', dim R^{k x k}. Equation layer is correct.
- **[OK] Capability gating is correct (over-claim guard + required-data guard)**
  - Poisson fit (family='poisson') is correctly blocked: symbolize() aborts with 'cannot read MCMCglmm / poisson / mu yet (status: Planned or reserved)', matching capabilities.csv row 'MCMCglmm,*,*,Planned or reserved'. Calling symbolize(fit) without data aborts: 'data is required for symbolize.MCMCglmm()'. Both clean cli errors.

**Verdict:** MCMCglmm is genuinely supported end-to-end for its three claimed capabilities and the equation/numeric layers are correct. I ran three live fits in /Users/z3437171/Dropbox/Github Local/symbolizer-hotfix (symbolizer 0.22.3, MCMCglmm 2.36): (1) MCMCglmm(body_mass ~ temperature, data=dat, family='gaussian', nitt=1500,burnin=500,thin=2,verbose=FALSE); (2) the same with random=~g (10 groups); (3) MCMCglmm(phenotype ~ 1, random=~animal, ginverse=list(animal=MCMCglmm::inverseA(ped)$Ainv), ...). symbolize(fit, data=dat) and as_latex(sym) ran without error in all three. The Gaussian likelihood (Normal, identity link), fixed effects (matched to summary(fit)$solutions with credible intervals, ci_method='credible'), variance components (SD = sqrt(Gcovariance/Rcovariance post.mean)), the random-intercept line u_g ~ N(0, sigma_g^2), the animal-model line u_animal ~ N(0, sigma^2 A) with the structured A matrix (not I), n_levels = nrow(Ainv), and the derived heritability h^2 = 0.735 are all mathematically correct. Gating is sound: non-Gaussian (Poisson) and missing-data both abort cleanly, matching the 'Planned or reserved' registry row. registry_match = partial because of one MAJOR text defect: the pedigree animal-model path renders phylo assumption prose from inst/extdata/assumption-templates.csv:149-153 that is tree-specific ('species-level', 'shared branch length / total tree height', 'tips-only', 'Brownian motion', 'ultrametric tree') and therefore wrong for a pedigree A, and the 'tips-only' row directly contradicts the extractor's own metadata$phylo_representation='all_nodes' and the symbol-dictionary 'all-nodes' description. This ships on the explicitly-claimed MCMCglmm,gaussian,animal (First slice) capability, and test-symbolize-mcmcglmm-animal.R:60-71 locks in the row keys while leaving the misleading text unguarded. Recommend a pedigree-vs-tree (or all_nodes-vs-tips_only) split of the gaussian/phylo assumption template before relying on the methods/assumptions prose for animal models; the equations themselves are safe to use today.

## metafor  (symbolize=wrong_output, registry=overclaims)
Classes checked: rma.uni, rma.uni (rma.ls location-scale), rma.mv
- **[BLOCKER] Categorical moderator in rma.uni meta-regression crashes symbolize()**
  - rma(yi, vi, mods = ~ g, ...) where g is a 3-level factor -> symbolize(fit) errors: "object 'gb' not found". Root cause: symbolize.rma.uni (R/symbolize-metafor.R ~line 117-120) reconstructs the formula from fit$beta dummy row-names (intrcpt, gb, gc) as `yi ~ gb + gc`, then drm_build_terms -> extract_terms calls model.frame referencing nonexistent data columns gb/gc. fit$data DOES carry the original factor `g`, but the extractor ignores it and uses the design-matrix dummy names instead of the original formula (fit$call$mods / fit$formula.mods). Categorical moderators are the single most common meta-regression idiom and the registry claims rma.uni/meta_normal/mu as 'First slice' (capabilities.csv line 106). Numeric moderators (mods = ~ x) work fine; only factor moderators crash.
- **[MAJOR] rma.mv factor moderator silently mis-rendered as a continuous predictor (loses contrast structure)**
  - rma.mv(yi, vi, mods = ~ g2, random = ~1|trial) with binary factor g2 does NOT crash (symbolize.rma.mv copies fit$X dummy columns into data, lines 308-314), but the dummy g2y is rendered as role='predictor', variable='g2y', contrast_level=NA, LaTeX `\mu_i = \beta_0 + \beta_1 g2y_i`. It should be a factor contrast (role='contrast', variable='g2', contrast_level='y'). parameter_interpretation would read 'the true effect changes by 0.276 per unit of g2y' -- wrong framing for a y-vs-x contrast. Same root cause as the rma.uni crash: formula synthesized from design-matrix dummy names, not the original factor formula.
- **[MAJOR] No-intercept formula (~ x - 1 / ~ outcome - 1) injects a phantom beta_0 into fixed_effects, LaTeX, and interpretation prose**
  - For intercept-free models fit$beta has no 'intrcpt' row, but symbolize synthesizes `yi ~ <preds>` (no `- 1`), so extract_terms adds an implicit (Intercept) with no matching coefficient. (a) rma(yi,vi,mods=~xn-1): fixed_effects shows (Intercept) beta_{0} estimate=NA + real slope mislabeled beta_{1}; as_latex emits `\mu_i = \beta_{0} + \beta_{1} xn_i + ...` -- an intercept the model never estimated. (b) The canonical bivariate UN idiom rma.mv(yi,vi,mods=~outcome-1,random=~outcome|trial,struct='UN') (Berkey-1998 cell-means; matches the package's own helper fixture fit_metafor_rma_mv_un which uses mods=~outcome-1): as_latex shows `\beta_{0} + \beta_{1} outcomeA_i + \beta_{2} outcomeB_i`, and parameter_interpretation() prints `(Intercept) [intercept] estimate=" NA"` with a full natural-language reading plus the two cell-means mislabeled as 'slopes' ('changes by 0.158 per unit of outcomeA'). Registry claims struct_UN 'First slice' and explicitly names the 'Bivariate / multivariate meta-analysis pattern' (capabilities.csv line 112). Existing UN tests (test-symbolize-metafor-un.R) check only variance_components, never fixed_effects, so this is unguarded.
- **[MINOR] rma.ls LaTeX heterogeneity line inconsistent with the per-observation scale model**
  - as_latex on rma(...,scale=~x) renders `\log(\tau^{2}_i) = \alpha_0 + \alpha_1 x_i` (correct, per-observation) but the random-effect distribution line below still reads `u_{study} ~ N(0, \sigma_{study}^2)` -- a single constant variance, contradicting the per-i scale model directly above. The variance IS tau^2_i in a location-scale model.
- **[NIT] Notation mixing: theta vs mu on LHS, and tau^2 (tables) vs sigma^2 (LaTeX)**
  - Distribution line uses theta_i (`y_i | \theta_i ~ Normal(\theta_i, v_i)`) but the linear-predictor LHS is `\mu_i = ...`; standard meta-analysis writes theta_i = beta_0 + ... + u_i, so the LHS would be more consistent as theta_i. Separately, variance_components$term uses 'tau^2' while as_latex writes '\sigma_{study}^2' for the same heterogeneity. Both are valid notation, but mixing within one object can confuse readers.
- **[OK] rma.uni random-effects + numeric meta-regression + rma.ls location-scale all mathematically correct**
  - rma(yi,vi): tau^2 in variance_components = 0.365 matches fit$tau2; beta_0/SE/CI correct; LaTeX two-level `y_i|theta_i ~ Normal(theta_i,v_i), v_i known` + `mu_i = beta_0 + u_{study(i)}` + `u_{study} ~ N(0,sigma_study^2)` is correct. mods=~x: beta=(0.412,0.535) matches coef(fit). rma.ls: the subtle metafor-specific parameterization log(tau^2_i)=alpha_0+alpha_1 x (log of VARIANCE, alpha family, link='log') is exactly right; beta and alpha estimates match coef(fit)$beta / $alpha; submodels table correctly tags tau2 link=log coef_family=alpha.
- **[OK] rma.mv multilevel, phylogenetic R-matrix, and UN variance components correct**
  - ~1|district/study: nested syntax unrolls to two tiers (district, district/study); sigma^2 estimates match fit$sigma2; LaTeX shows both u_{district(i)} and u_{district/study(i)}. Phylo rma.mv(...,R=list(species=A)): detected_signals='phylo', phylo_representation='tips_only', structured_random captures 12x12 R-matrix, LaTeX correctly renders `\mathbf{u}_{species} ~ N(\mathbf{0}, \sigma_{species}^2 \mathbf{A})` (phylo correlation matrix A, not identity). UN bivariate: variance_components correctly produces kind='heterogeneity_un' (tau^2_A, tau^2_B) + kind='correlation' (rho_A_B) + sampling_variance rows. All 63 existing metafor tests pass.

**Verdict:** metafor support is broad and mostly correct on the "happy path" but has a BLOCKER on the most common meta-regression idiom. VERIFIED CORRECT end-to-end: rma.uni random-effects (yi~1), rma.uni meta-regression with NUMERIC moderators, rma.ls location-scale (the subtle log(tau^2_i)=alpha_0+alpha_k z_k log-variance/alpha parameterization is exactly right and matches Viechtbauer&Lopez-Lopez 2022 / Nakagawa et al. 2025), rma.mv multilevel (nested ~1|district/study unrolling + per-tier sigma^2), rma.mv phylogenetic (R=list(species=A) -> u~N(0,sigma^2 A), correct phylo detection + tips-only), and rma.mv UN variance components (heterogeneity_un + correlation rows). All 63 existing metafor tests pass. DEFECTS: (1) BLOCKER -- a CATEGORICAL moderator in rma.uni crashes symbolize() with \"object '<dummy>' not found\" because the extractor rebuilds the formula from fit$beta design-matrix dummy row-names (gb, gc) instead of the original factor formula; this is the standard categorical meta-regression. (2) MAJOR -- the same root cause makes rma.mv silently mis-render a factor moderator as a continuous \\beta_1 dummy_i slope, losing contrast structure. (3) MAJOR -- no-intercept formulas (~ x - 1, and the canonical bivariate ~ outcome - 1 UN idiom that the package's own fixture uses) inject a phantom \\beta_0 (estimate NA) into fixed_effects, as_latex(), AND parameter_interpretation() prose, mislabeling the real cell-means coefficients; unguarded because the UN tests never inspect fixed_effects. Registry verdict = OVERCLAIMS: rma.uni/meta_normal/mu and rma.mv/meta_normal/struct_UN are marked 'First slice', and the struct_UN row explicitly advertises the 'Bivariate / multivariate meta-analysis pattern', yet the canonical realizations (factor moderators; ~ outcome - 1 cell-means) crash or mis-render. EXACT FIT CODE for the BLOCKER: set.seed(1); k<-30; vi<-runif(k,.02,.15); g<-factor(sample(c('a','b','c'),k,TRUE)); yi<-c(a=.1,b=.4,c=.7)[as.character(g)]+rnorm(k,0,sqrt(.05))+rnorm(k,0,sqrt(vi)); d<-data.frame(yi,vi,g); fit<-metafor::rma(yi,vi,mods=~g,data=d,method='REML'); symbolizer::symbolize(fit)  # Error: object 'gb' not found. The fix for all three (1-3) is the same: synthesize the moderator formula from the original model formula (fit$call$mods / fit$formula.mods) and use fit$data, preserving factor terms and the intercept/no-intercept structure, rather than reconstructing it from colnames(fit$X)/rownames(fit$beta) dummy names.

## phylolm  (symbolize=wrong_output, registry=overclaims)
Classes checked: phylolm
- **[MAJOR] Matrix-form distribution asserts a DIAGONAL residual covariance, which is wrong for PGLS**
  - explain(fit) renders the matrix-form distribution as: \mathbf{y} | \boldsymbol{\mu},\boldsymbol{\sigma} \sim \mathcal{N}(\boldsymbol{\mu}, \mathrm{diag}(\boldsymbol{\sigma}^2)). PGLS has a DENSE covariance sigma^2 * C(alpha) (the registry's own claim, capabilities.csv:126). Source: phylolm reuses the generic Gaussian template inst/extdata/family-distributions.csv:2 ('...\mathcal{N}(\boldsymbol{\mu}, \mathrm{diag}(\boldsymbol{\sigma}^2))') with no phylolm override in R/symbolize-phylolm.R (drm_build_distribution called at line 150). The phylogenetic correlation -- the defining feature of the model -- is rendered as its mathematical opposite (independent residuals).
- **[MAJOR] as_latex() model equation omits the phylogenetic correlation entirely (both notations)**
  - as_latex(sym) for BM and for lambda produce IDENTICAL output to an ordinary OLS:
\begin{aligned}
y_i | \mu_i,\sigma_i & \sim \mathrm{Normal}(\mu_i,\sigma_i^2) \\
\mu_i & = \beta_0 + \beta_1 x_i
\end{aligned}
No C(alpha), no A, no lambda (0.8455) appears. The headline LaTeX product gives no indication the residuals are phylogenetically correlated; A survives only as an orphan symbol_dictionary row never wired into the equation.
- **[MAJOR] Assumptions table self-contradicts: an 'independence' row fires alongside the phylo covariance**
  - sym$assumptions contains BOTH assumption='independence' (expr 'y_i \perp y_j \mid X \text{ for } i \ne j', meaning 'Observations are conditionally independent given the predictors') AND assumption='phylo_random_effect' (covariance proportional to A). For PGLS the independence assumption is false by construction. Root cause: drm_build_assumptions in R/symbolize-drmtmb.R:1923-1925 drops 'independence' only when has_re is TRUE; phylolm sets re_tbl=NULL (symbolize-phylolm.R:157), so has_re=FALSE and the plain independence row is retained even though detected_signals='phylo'. The 'independence' template row (assumption-templates.csv:5) carries no 'requires' tag so signal-gating does not remove it.
- **[MINOR] Phylo assumption row misattributes the structure to a random effect u_p that phylolm does not have**
  - The fired phylo row states \mathbf{u}_p \sim \mathcal{N}(\mathbf{0}, \sigma_p^2 \mathbf{A}) ('Species-level random effect ...'). But symbolize-phylolm.R header (lines 25-28) and the extractor itself document that phylolm has NO u_p random effect -- the signal is marginalized into the residual (phylo_representation='pgls_marginal'). The RE-style prose was authored for the brms/MCMCglmm tips_only case (separate u_p + sigma_res^2 I) and is mislabeled when reused for the marginal PGLS form. Contrast: symbolize.brmsfit sets phylo_representation='tips_only' where N(mu,diag) and a u_p row ARE correct.
- **[MINOR] Spurious sigma scale-submodel assumption rows for a model with a single scalar sigma2**
  - sym$assumptions includes submodel='sigma' rows: linear_predictor '\log(\sigma_i) = \gamma_0 + \sum_k \gamma_k Z_{ki}' ('Log residual SD ... is a linear combination of the scale-model predictors') and positivity '\sigma_i > 0' ('constrained positive via the log link'). phylolm has one scalar sigma2 (=0.2485) and no distributional/scale submodel; these generic-Gaussian boilerplate rows describe machinery (gamma coefficients, Z design, log link on sigma) that does not exist in a phylolm fit.
- **[MINOR] methods_text() unsupported for phylolm (acceptable, but a prose surface gap)**
  - methods_text(sym) aborts: 'No methods_text() template for class "phylolm" / family "gaussian".' with a clean cli list of supported combos. Not an overclaim (registry does not list a methods component for phylolm) and the message is informative, but the phylo Methods narrative -- where the marginal-covariance bridge prose is most needed -- is absent.
- **[OK] symbolize.phylolm runs and core numerics are exact (BM + lambda)**
  - For y~x on a 12-tip rcoal tree, symbolize(fit, data=dat) returns a symbolized_model with model$class='phylolm', family='gaussian', n_obs=12. fixed_effects estimates equal coef(fit) ((Intercept)=-0.690, x=-0.0262), std_error == sqrt(diag(vcov(fit))) exactly, confint_low/high == estimate +/- 1.96*SE (all.equal TRUE, tol 1e-10), ci_method='wald', excludes_zero correct (FALSE,FALSE). variance_components sigma^2 row = fit$sigma2 = 0.2485 exactly. metadata phylo_representation='pgls_marginal', phylo_model='BM'/'lambda', phylo_param captured (NA for BM; 0.8455 = fit$optpar for lambda). All 31 existing tests in tests/testthat/test-symbolize-phylolm.R pass.

**Verdict:** phylolm is installed and symbolize.phylolm runs end-to-end on live BM and Pagel's-lambda fits with EXACT numerics: fixed-effects estimates, Wald SEs, +/-1.96*SE CIs, excludes_zero, the sigma^2 variance component, and the phylo metadata (phylo_representation='pgls_marginal', phylo_model, phylo_param=fit$optpar) all match the fitted object, and all 31 existing tests pass. However, the RENDERED MODEL is mathematically wrong for PGLS, so I rate registry_match=overclaims and symbolize_status=wrong_output. The capability registry (inst/extdata/capabilities.csv:126-127) advertises phylolm/gaussian/mu and phylolm/*/phylo as 'First slice' supporting the 'PGLS marginal form y = X*beta + e, e ~ N(0, sigma^2 * C(alpha))', but the actual output contradicts that exact form: (1) the matrix-form distribution asserts N(mu, diag(sigma^2)) -- a diagonal/independent covariance, the opposite of the dense phylogenetic C(alpha); (2) as_latex() in both index and matrix notation is identical to ordinary OLS, with no C(alpha)/A/lambda anywhere, so the phylogeny is invisible in the headline equation; (3) the assumptions block self-contradicts, firing an 'independence' row ('Observations are conditionally independent given the predictors') next to the phylo-covariance row. The root cause of (3) is R/symbolize-drmtmb.R:1923-1925, which only drops the 'independence' assumption when a random-effect tibble exists; phylolm correctly carries re_tbl=NULL (the signal is in the residual, not a u_p), so the independence row is wrongly retained, and the phylo rows themselves are mislabeled as a 'u_p random effect' that phylolm does not have. Two further MINORs: spurious sigma scale-submodel rows (log(sigma_i)=gamma_0+... ) for a model with one scalar sigma2, and methods_text() is unsupported. Exact fit code used: set.seed(31); k<-12; tree<-ape::rcoal(k); tree$tip.label<-paste0('sp',1:k); C<-ape::vcv(tree,corr=TRUE); y<-as.numeric(crossprod(chol(C),rnorm(k))); x<-rnorm(k); dat<-data.frame(y=y,x=x,row.names=tree$tip.label); fit<-phylolm::phylolm(y~x,data=dat,phy=tree,model='BM'); sym<-symbolize(fit,data=dat); as_latex(sym). The numeric extraction layer is solid and trustworthy; the prose/equation layer over-claims a PGLS marginal-covariance representation it does not actually render and in places states the mathematically opposite (diagonal/independent) structure -- this should be downgraded in the registry or the renderers fixed to surface sigma^2 C(alpha) and suppress the independence row when detected_signals contains 'phylo'.

## sdmTMB  (symbolize=works, registry=overclaims)
Classes checked: sdmTMB
- **[MAJOR] Spatial/spatiotemporal random fields rendered as plain IID random intercepts, not the GMRF the registry promises**
  - Registry rows 88-89 of inst/extdata/capabilities.csv and the source header (R/symbolize-sdmtmb.R lines 24-28) explicitly claim 'omega(s_i) ~ GMRF(0, Q(kappa))' and 'epsilon(s_i,t) ~ GMRF(0, Q(kappa))' with kappa=sqrt(8)/range. But every rendered output shows an ordinary exchangeable random intercept: as_latex gives 'u_{site} \sim \mathcal{N}(0,\, \sigma_{site}^2)' (matrix form '\mathbf{u}_{site} \sim \mathcal{N}(\mathbf{0},\, \sigma_{site}^2 \mathbf{I})'). A grep over the LaTeX confirms GMRF=FALSE, SPDE=FALSE, Matern=FALSE, kappa=FALSE, Q(=FALSE. The 'range' parameter is extracted into variance_components (kind=spatial_range) but never appears in any equation. sdmtmb_build_re_per_entry() (lines 312-336) hard-codes term_label '(1 | site)' and drm_build_random_effects() then renders it identically to a glmmTMB IID intercept. Net effect: spatial autocorrelation -- the defining feature of an sdmTMB model -- is mathematically misrepresented as independence.
- **[MAJOR] Delta / two-component models crash with opaque base-R error instead of the intended 'Planned or reserved' routing**
  - Registry row 90 marks sdmTMB delta_model as 'Planned or reserved' and R/symbolize-sdmtmb.R lines 81-83 contain a capability_check('sdmTMB', family, 'delta_model'). But a live delta_gamma() fit sets fit$family$family to the length-2 vector c('binomial','Gamma'); line 67 'is.null(family) || !nzchar(family)' then throws "'length = 2' in coercion to 'logical(1)'" (confirmed via conditionCall = is.null(family) || !nzchar(family)) BEFORE the delta_model gate at line 81 is reached. Users get a confusing internal coercion error rather than the clean capability message. Fit code: sdmTMB::sdmTMB(obs ~ z, data=dat4, mesh=mesh4, family=delta_gamma(), spatial='on').
- **[MINOR] AR1 spatiotemporal correlation parameter rho is bucketed as 'other' and the AR1 structure is invisible**
  - For a spatiotemporal='ar1' fit, sdmTMB::tidy(fit,'ran_pars') returns a 'rho' row (AR1 temporal correlation, e.g. -0.727). sdmtmb_param_group()/sdmtmb_param_kind() (lines 372-392) have no case for 'rho', so it maps to group='other', kind='other'. It is carried in variance_components but never surfaced as a temporal-correlation parameter, and the epsilon field renders with the same IID 'u_{site_time} ~ N(0, sigma_site_time^2)' form as the spatial-only case -- the AR1 temporal dependence does not appear anywhere in the equation.
- **[NIT] Literal NA leaks into user-facing matrix LaTeX for the random field**
  - Because the field's n_levels is NA_integer_ (set in sdmtmb_build_re_per_entry), the matrix-form equation renders as '\mathbf{u}_{site} \sim \mathcal{N}(\mathbf{0},\, \sigma_{site}^2 \mathbf{I}_{NA})' and the symbol_dictionary dimension shows '\mathbb{R}^{NA}'. Cosmetic but 'NA' appears verbatim in output a user would paste into a manuscript.
- **[OK] Gaussian conditional submodel, fixed effects, Wald CIs, and variance components extracted correctly**
  - Live Gaussian+spatial fit: distribution = Normal(mu_i, sigma_i^2) with identity link (correct). fixed_effects beta_0=1.94 (1.81,2.06), beta_1(z)=0.543 (0.425,0.661) match sdmTMB::tidy(fit, conf.int=TRUE) exactly; ci_method='wald'. variance_components carries range, phi (residual, sd=1.087), sigma_O (spatial_random) for the spatial fit and additionally sigma_E (spatiotemporal_random) for the AR1 fit -- all numerically matching tidy(fit,'ran_pars'). methods_text and parameter_interpretation are coherent and non-misleading for the mean structure.
- **[OK] Non-Gaussian families cleanly gated and the test suite passes**
  - A live Tweedie fit (family=tweedie(link='log'), spatial='on') is correctly rejected via capability_check with the message "symbolize() cannot read sdmTMB / tweedie / mu yet (status: Planned or reserved)" matching registry row 91 (the '*,*' wildcard). devtools::load_all + testthat::test_file on tests/testthat/test-symbolize-sdmtmb.R reports all green ('Your tests are lit'); the 'NaNs produced' lines are warnings from the unstable toy mesh, not failures.

**Verdict:** symbolizer's sdmTMB support is real and the green path is correct for the GAUSSIAN MEAN structure: symbolize() runs, the Normal/identity distribution is right, fixed effects with Wald CIs and the variance components (range, sigma_O, sigma_E, phi) are extracted accurately and match sdmTMB::tidy(). Fit code used (live, fast): set.seed(1); n<-300; dat<-data.frame(x=runif(n),y_loc=runif(n),z=rnorm(n)); dat$obs<-2+0.5*dat$z+rnorm(n,0,1); mesh<-sdmTMB::make_mesh(dat,c('x','y_loc'),cutoff=0.1); fit<-sdmTMB::sdmTMB(obs~z,data=dat,mesh=mesh,family=gaussian(),spatial='on') (plus an AR1 spatiotemporal variant with time='year', a Tweedie variant, and a delta_gamma() variant). However the registry OVERCLAIMS on the part that matters most for this package: the omega/epsilon rows are sold as 'First slice' GMRF spatial/spatiotemporal fields (omega(s_i) ~ GMRF(0, Q(kappa))), but the renderer emits an ordinary IID random intercept u_{site} ~ N(0, sigma_site^2) with no GMRF/SPDE/Matern/kappa/range anywhere in any equation -- spatial autocorrelation is rendered as independence, which is mathematically misleading for the package's signature model class (MAJOR). Separately, delta models -- which the registry marks 'Planned or reserved' -- do not route cleanly: delta_gamma() yields a length-2 family vector that crashes line 67 (is.null(family) || !nzchar(family)) with \"'length = 2' in coercion to 'logical(1)'\" before the delta_model gate runs (MAJOR). Minor issues: AR1 rho is bucketed as 'other' and the AR1 temporal structure is invisible, and a literal 'NA' leaks into the matrix-form LaTeX (\\mathbf{I}_{NA}). Recommendation: treat the spatial-field rendering as the priority fix (either render the GMRF/SPDE form the header documents, or downgrade the omega/epsilon claims) and harden the family-resolution/delta gating so two-component fits get the intended capability message instead of an internal error.
