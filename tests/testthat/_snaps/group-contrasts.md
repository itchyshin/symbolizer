# print method names the method and the null

    Code
      print(gc)
    Message
      
      -- Group contrasts (pairwise) --
      
      A / B estimate = "0.991" (0.756, 1.30)
      A / C estimate = "0.827" (0.638, 1.07)
      B / C estimate = "0.835" (0.645, 1.08)
      Values are ratios; the null is 1, not 0. Intervals are per-contrast, not
      family-wise; pass adjust = "tukey" for simultaneous bands. Scale: response. CI
      method: wald. Adjustment: none. Rows marked `*` have a 95% interval that
      excludes the null.

